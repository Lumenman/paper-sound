# Лабораторный журнал

> Команды в записях ниже вызывают `paper_sound.py`, `bench.py` и прочее из
> корня: так лежали файлы, когда эти опыты ставились. Теперь программа
> живёт в `paper-sound/`, то есть `python paper-sound/bench.py ...`.

Не документация. README описывает то, что формат делает; здесь то, что про него
измерено, но ещё не решено, и что предстоит проверить на бумаге.

Метрика везде та же, что в README: `dB = 10·log10(r²/(1−r²))`, где `r` —
корреляция чтения с тем звуком, что печатали.

**Что здесь симуляция, а что бумага.** Всё, что ниже помечено «сим.», получено
на отрендеренном листе, прогнанном через модель скана: рябь каретки 1.5 отсчёта
rms на 30 циклов по листу, боксовое размытие, гауссов шум. Ориентир: два
настоящих листа из README стоят на 1.9 и 8.7 dB, то есть между строками
«blur 5, шум 10» и «blur 7, шум 20» — реальная деградация живёт в нижней
половине всех таблиц ниже.

**26.08 бумага появилась.** Пять листов нового формата напечатаны и
отсканированы; всё, что они сказали, лежит в §3, и там же поправки к тому, что
симуляция предсказала неверно. Симулированные таблицы §2 оставлены как были —
сравнивать их с §3 полезно.

---

## 1. Сделано и лежит в коде

| что было | что стало |
|---|---|
| `pilot_retime` резал голову независимо от того, на каком конце нашлись часы | одни часы с хвоста → секунда звука выброшена спереди, полоса часов осталась в wav как тон. Теперь `ends()` помнит конец. Проверка в selftest |
| `pilot_timebase` не снимал прямую, хотя `pilot_bin` снимал | перекос в трассе тащил оценку ряби: при 18 px (кривой лист из README) \|r\| 0.928 → 0.996 |
| `find_tracks` ограничивал чернила полосы только снизу | добавлен верх, `0.25·median < l < 1.4·median`. `04.png`: было 125 полос / 1 часы / рябь 6.06 rms, стало 120 / 2 / 2.33 |
| «only one clock» было расплывчатым | теперь ищет часы по шесть полос вглубь от краёв и называет их: «clocks on lanes 0 and 121 of 126 — сетка перебрала поле на 0 слева и 4 справа» |
| `write_wav` нормировал по пику: один сорванный лан уводил вниз весь файл | нормировка по 99.9-му перцентилю с запасом `HEADROOM = 1.3`. Уровень пяти сканов сошёлся в −14.8…−15.4 dBFS при источнике −15.5, на 0004 это +8.8 dB. Здоровый файл не клипается вообще, см. 3.5 |
| `find_pitch` мерил шаг одним числом на лист | медиана по третям, если в трети хотя бы восемь ланов. `04_1.png`: было 243 лана / шаг 18.4376 / 13 с шума / сдвиг −0.5 с, стало 240 / 18.4642 / чисто, и `--pitch` руками больше не нужен |
| `find_pitch` решал спектром, лан это или его гармоника. Порог `SUBHARM = 0.5` популяции не разделяет: у листа с забитым фундаменталом отношение 0.235, у здорового доходит до 0.245 | решает бумага. `harmonic()` берёт медиану чернил на линиях реза для шага, ×2 и ×3 и оставляет **наименьший достаточно чистый**. Вопрос о субгармонике убран из третей (в окне в десять ланов бин 4 — не решётка, а огибающая: 0.74…0.93 против 0.33…0.39 у того же листа целиком), медиана третей получила вето 3%. На синтетике 15/24 правильных листов → **20/24**, пять реальных сканов не сдвинулись ни в одной цифре |
| лан, выпавший из сегментации, молча выбрасывался: файл короче на секунду, всё после дыры играет на секунду раньше | на его место встаёт секунда тишины, и номер называется. Лист из 20 ланов с вытертым девятым: было 19 ланов и 0.04 до конца страницы, стало 20 и 1.00 |
| скив считался дважды — в `read_curves_page` и в `cut_lanes.traces()`; предупреждение о «лане, который не лан» жило двумя копиями | общие `lane_drift()` и `odd_lane()` |
| пустая страница падала traceback'ом из `inked_span` | `ValueError("no ink on this page")`, и резак ловит его так же, как `paper_sound` |

Побочно: `cut_grid()` вынесен из `find_tracks` (стоимость реза нужна теперь
двоим) и считает **медиану**, а не среднее — одна линия, попавшая в сплошной
край сканера, весит 15 единиц среднего и решала вопрос в одиночку, а медиана
сдвигается только когда грязна половина линий, что и есть половинный шаг.
`write_wav` на полной тишине больше не делит на ноль, `load_ink` не держит
дескриптор, `runs_of` и `mark_centre` удалены как мёртвые, `do_print`
предупреждает, когда `--dpi` делает штрих тоньше 0.15 мм (1200 dpi даёт
0.085 мм — струйник кладёт такое дыряво), а `score.py`/`align.py`/`spec.py`
перестали ломаться о букву диска в путях. Комментарии к `BASELINE` и `DECLICK`
приведены к замеру: это **8.8 Гц и 2.4 мс** на A4@600, а не 20 Гц и 1 мс;
сами числа не тронуты — по всем бумагам и dpi корень уезжает в 4.4…17.7 Гц,
что ниже слышимого.

`bench.py` теперь печатает **какие** секунды ниже 0.5, сворачивая подряд идущие
в диапазон: россыпь одиночек — беда подорожечная (смотреть полоски из
`cut_lanes.py`), один длинный кусок — бумага: складка, полоса ряби, край
стекла.

Побочно: метка в selftest перерисована на ⅔ чернил (в полную силу её теперь
отбрасывает вес, и проверка ширины мерила бы пустоту), добавлена проверка на
сам верхний порог — без него сплошной край даёт 13 полос вместо 11. Оба README
обновлены.

Механику по «only one clock» не доделывали: найдя часы на 121-й, ридер их
по-прежнему не использует и хвост не режет. После верхнего порога этот путь на
здешних сканах не срабатывает.

---

## 2. Измерено, не внедрено

### 2.1 Растр: 1 бит против полутонов (сим.)

Один лист, пять способов растеризации, одинаковый скан. «Растр …» — то, что
сделает с серым файлом драйвер принтера, если отдать ему полутона.

| растр | чернил | blur 2, шум 3 | blur 3, шум 4 | blur 5, шум 10 |
|---|---|---|---|---|
| серый (как отдаётся сейчас) | 9.79% | 12.01 dB | 14.39 dB | 12.31 dB |
| 1 бит, порог 0.5 | 9.79% | 11.72 | 13.99 | 12.11 |
| растр кластерный, ~100 lpi | 11.86% | 11.80 | 14.12 | 12.20 |
| растр Байера 8×8 | 9.84% | 11.77 | 14.06 | 12.03 |
| диффузия ошибки | 9.79% | 11.87 | 14.18 | 12.20 |

Разброс 0.3 dB на весь столбец: положение плиты в 4 px переживает любую
бинаризацию, а размытие скана возвращает полутон обратно.

На листе, не видевшем принтера: серый **32.91 dB**, битональный **20.41**. Это
свойство стенда, а не бумаги, и 20.4 всё равно на 11 dB выше того, что отдаёт
бумага — то есть `bench` от перехода на 1 бит не слепнет, вопреки комментарию
в `render_page`.

**Вывод.** 1-битный растр стоит делать не ради качества (его там нет), а ради
контроля: он убирает из цепочки решение драйвера, которое сейчас принимается
без нас и не воспроизводится. Серый рендер оставить для `selftest`/`bench`.

### 2.2 Скорость бумаги (сим.)

Высота полосы в строках — это линейная скорость носителя, как обороты
пластинки. Сейчас 6780 строк на секунду. Полоса в 1/N секунды = скорость ×N.
Полоса звука во всех строках одна и та же, полные 3390 Гц:

| скорость | строк/отсчёт | секунд на A4 | blur 2, шум 3 | blur 3, шум 4 | blur 5, шум 10 |
|---|---|---|---|---|---|
| ×1 (сейчас) | 1.00 | 120 | 12.01 dB | 14.40 dB | 12.32 dB |
| ×4/3 | 1.33 | 90 | 13.07 | 14.74 | 12.69 |
| ×3/2 | 1.50 | 80 | 13.09 | 14.39 | 12.25 |
| **×2** | 2.00 | **60** | **14.42** | **15.53** | **13.93** |
| ×3 | 3.00 | 40 | 15.39 | 15.94 | 14.09 |

Ясно платит только шаг до ×2: **+1.1…2.4 dB за половину листа.** ×3 добавляет
около децибела.

> **Бумага (§3.2): +1.9 dB.** Попадание в верхний край вилки, модель по этой
> статье не врёт. Но §3.3: те же полбумаги, вложенные в ширину лана, а не в
> строки, по всем признакам дают больше, и таблица выше этого сравнения не
> делает — в ней шаг стоит на месте.

Вторая, другая сделка — срезать верх полосы (3390 → 1695 Гц) и оставить 120
секунд на листе — даёт больше, +2…7 dB, но часть этих децибел получена тем, что
у бумаги перестали просить самое трудное. Для речи это неправильная сторона
размена: весь `prep.bat` заточен на 3.4 кГц (подъём +4 dB на 2600 Гц). Чернила
на бумаге в обеих сделках одинаковые, отличается только число в заголовке wav.

### 2.3 Скорость может быть вшита в лист, без метаданных

Часы печатаются с периодом в **отсчётах** (32 и 20). При N строках на отсчёт их
период на бумаге равен `per × N` строк — а этот период ридер и так измеряет.
Что получилось бы на имеющихся сканах, если бы он делил:

| лист | sr | N по левым часам | N по правым |
|---|---|---|---|
| 01–03 | 6573–6576 | 0.969 | 0.969 |
| 04 | 6598 | 0.982 | 0.991 |
| 05 | 6596 | 0.972 | 0.972 |
| 06 | 6590 | 0.971 | 0.971 |
| sheet1 (рендер) | 6780 | 0.999 | 1.000 |

Худшее отклонение 3.1% против ±25%, которые терпит округление. Старые листы
измеряются как N=1 и читаются ровно как сейчас.

Допустимый набор N — {1, 2, 4}: кандидаты, которые ридер должен различать, это
все пары (период, N), и они не должны сойтись ближе ошибки масштаба.

| PILOT_END | периоды-кандидаты, строк | худший зазор |
|---|---|---|
| 20 (сейчас) | 20, 32, 40, 64, 80, 128 | ×1.25 = 25% |
| 23 = 32/√2 | 23, 32, 46, 64, 92, 128 | ×1.39 = 39% |

N=3 в лестницу не лезет: 32×3 = 96 против 20×5 = 100, зазор 4%. `PILOT_END =
23` пока бесплатен — реальных листов нового формата не существует.

> **После бумаги (§3.3) набор сужается до {1, 2}.** N=4 уводит на 30 секунд с
> листа ради примерно +3 dB и раньше упрётся в то, во что упёрся 0004. С
> набором {1, 2} кандидаты — 20, 32, 40, 64: худший зазор ×1.25 и без
> `PILOT_END = 23`, то есть менять его незачем, а освободившийся запас
> точности лучше оставить себе. И сначала — лист с удвоенным шагом (§3.3):
> если он подтвердит +3 dB, вся эта механика может не понадобиться вовсе.
> Пять реальных листов теперь есть, и `PILOT_END = 23` на них уже не бесплатен.

**Соглашение, которое лист выдать не может** (чернила в обеих сделках
одинаковые): частота дискретизации всегда равна высоте чернил в строках, полоса
держит 1/N секунды. При N=1 это сегодняшнее поведение слово в слово.

**Что придётся переставить в коде.** N измеряется по часам, часы читаются из
полосы в полный рост — значит прореживание идёт после чтения часов, а сейчас
`read_curves_page` ресемплит все полосы к общему `sr` до того, как `pilot_retime`
их увидит. И `resample()` — голая `np.interp`: прореживание 6780 → 3390 без
антиалиасного фильтра сложит шум скана обратно в полосу. `to_rate` фильтр уже
умеет.

`read` при этом не получает никакого флага: `--pitch` и N измеряются с листа по
горизонтали и по вертикали. Флаг нужен только `print` — выбрать, чем платить.

---

## 3. Скорость ×2 на бумаге: проведено 26.08

Пять листов напечатаны одним прогоном, отсканированы одной сессией
(01, 02, 03, 04_1 в 18:58–19:03, 05 отдельно в 22:15). Рябь каретки
0.93–1.37 отсчёта rms против 2.33 на старом `04.png` — сессия чистая.

Все пять листов легли на стекло вверх ногами, и все пять ридер развернул
правильно. Единственное катастрофическое место ридера теперь измерено на
бумаге: 5 из 5. Пункт из §4 закрыт.

| лист | скан | звук | строк/отсчёт | шаг, px | секунд звука на A4 |
|---|---|---|---|---|---|
| 0001 | 01 | out3 | 1 | 38.7 | 120 — **контроль** |
| 0002 | 02 | out3 | 1 | 19.0 | 120 на половине листа |
| 0003 | 03 | slow3 | 2 | 38.7 | 60 |
| 0004 | 04_1 | slow3 | 2 | 19.0 | 120 |
| 0005 | 05 | slow1 | 2 | 38.7 | 60 |

Медленные листы подняты обратно `sox speed 2 rate <sr>` и считаны против
общего `out3.wav` (05 — против `out1.wav`, он печатался с другого препа). Обе
ямы протокола обойдены: против `slow*.wav` не сравнивалось ничего, обратное
ускорение делал sox.

### 3.1 Метрик оказалось две, и разница между ними — тоже результат

`bench.py` снимает **один** лаг на весь лист. Это правильно ровно до тех пор,
пока привязка каждого лана к своему отсчёту точна: где она уходит на отсчёт,
общий лаг спасти не может, и ошибка привязки записывается в шум чтения.

Поэтому вторая метрика (`align.py`): лаг ищется заново на каждой секунде в
окне ±3 с. Она меряет чтение самих чернил, вынося привязку за скобки.

- **strict** = как `bench.py`, один лаг. Это то, как файл звучит.
- **tracking** = лаг на секунду. Это то, насколько хорошо прочитаны чернила.

Разрыв между ними — цена ошибки привязки. Он мал везде, кроме полушаговых
листов, и это отдельная находка, см. 3.3.

### 3.2 Результат

Медиана посекундного r, одни и те же первые 59 с `out3` (05 — `out1`):

| лист | что напечатано | strict | tracking |
|---|---|---|---|
| 01 | 1 с/лан, шаг 37.6 — контроль | 10.59 dB | **11.04 dB** |
| 02 | 1 с/лан, шаг 18.4 | 1.70 | **7.04** |
| 03 | 0.5 с/лан, шаг 37.6 | 12.92 | **12.96** |
| 04_1 | 0.5 с/лан, шаг 18.4, `--pitch 18.4642` | 6.68 | **8.61** |
| 04_1 | то же, шаг измерен ридером | не выравнивается | 7.90 |
| 05 | 0.5 с/лан, шаг 37.6, звук out1 | 14.81 | **14.81** |

Три числа, и они складываются:

- удвоить строки на отсчёт (0.5 с на лан, шаг тот же): **+1.9 dB**, ценой
  половины листа;
- вдвое сузить лан (шаг 18.4): **−4.0 dB**, экономя половину листа;
- и то и другое сразу, то есть **та же бумага на секунду, что у контроля**:
  **−2.4 dB**.

Проверка на аддитивность: 11.04 − 4.00 + 1.92 = 8.96 против измеренных 8.61.
Сходится в пределах 0.35 dB — эффекты независимы, и по ним можно считать.

Симуляция §2.2 обещала за ×2 от +1.1 до +2.4 dB. Бумага дала +1.9. **Модель
скана по этой статье не врёт**, и правило решения из старого протокола
(«≥1 dB в пользу B») формально сработало.

### 3.3 Но выигрыш даёт не скорость, а бумага

Строки не приносят децибелов. Их приносит площадь листа, а строки — лишь один
из двух способов её потратить, и, судя по 0004, худший: при равной бумаге на
секунду ширина обходит строки на **2.4 dB**. `--rows 2` честно читается как
«60 секунд на A4 вместо 120 за +1.9 dB», и записывать его надо так, а не как
улучшение формата.

Курс несимметричный, и формат стоит **над коленом кривой**: вверх +1.9 dB за
удвоение бумаги, вниз −4.0 dB за её половину. Уплотнять лан нельзя.

**Часы на узком лане тупеют.** Разрыв strict/tracking: 0.45 dB на контроле,
0.04 на 03, 0 на 05 — и **5.3 dB на 02**. Полосы часов на полушаговом листе
тоже вдвое уже, retime промахивается на 1–2 отсчёта на лан, а при полосе до
3.4 кГц отсчёт — заметная доля периода. Это не шум чтения, это привязка, и в
первом заходе она была записана листу 02 в чернила: там стояло −8.9 dB,
правильное число −4.0.

**Третьей точки нет, и она самая интересная.** Обе ручки тратят одну бумагу:
при `--rows 2` секунда получает два лана по 38.7 px, при удвоенном шаге — один
лан в 77.4 px. Те же 77.4 px на секунду, те же 60 секунд на A4. Размах при
этом растёт быстрее шага, потому что штрих (4 px) и поля (1.5 px) —
фиксированный накладной расход, `amp = (шаг − 4)/2 − 1.5`:

| шаг | размах | доля лана | измерено |
|---|---|---|---|
| 18.4 | 5.7 px | 31% | −4.0 dB |
| 38.7 | 15.9 px | 41% | 0 (контроль) |
| 77.4 | 35.2 px | 45% | **не печаталось** |

Сужение вдвое отняло 8.9 dB размаха и стоило 4.0 dB результата — размах
переходит в результат с коэффициентом около 0.45. Расширение вдвое даёт +6.9
dB размаха, по тому же коэффициенту это **≈ +3.1 dB** против +1.9 у `--rows
2`. Экстраполяция по двум точкам, и она может не дожить до 77.4 px: ручки
бьются с разным шумом (строки — с вертикальным размытием и рябью каретки,
ширина — с горизонтальной зернистостью и растеканием чернил), и на широком
лане первым может стать что-то третье. Один лист закрывает вопрос:

```
sox\sox.exe out3.wav src60.wav trim 0 60
python paper_sound.py print src60.wav -o sheetC --pitch 77.4
```

**Печатать его раньше, чем строить `--rows`.** Если +3 dB подтвердятся, ручка
шага делает ручку строк почти ненужной: она даёт больше за ту же бумагу и не
требует ни `PILOT_END`, ни измерения N по часам, ни перестановки ресемплинга —
шаг ридер и так меряет с листа.

### 3.4 Лист 0004: чем именно кончается плотный лан

Ридер сказал сам, первой же строкой:

```
lane 242 of 243 is 7 px against a median of 18 -- something on this sheet is
being read as a lane that is not one, and every later second is shifted
```

Глобальная оценка шага — 18.4376, собственный шаг листа по третям — 18.4642 /
18.4585 / 18.5717. Глобальная ниже любой локальной, сетка идёт быстрее бумаги,
набегает лишний лан, в конце остаётся огрызок в 7 px. Лист лежал вверх ногами,
ридер развернул — огрызок оказался в начале воспроизведения. Карта
выравнивания:

```
r:    0.09 0.27 0.18 0.25 0.21 0.26 0.41 ... 0.79 0.88 0.93 0.95 ...
лаг:  +0.95 0.00 0.00 ... −0.50 −0.50 −0.50 −0.50 (до конца)
```

Первые ~13 секунд мусор, дальше всё встаёт со сдвигом ровно −0.50 с — один
лишний лан на листе в 240. `--pitch 18.4642` чинит: первые 10 секунд 0.22 →
0.76, сорванных ланов 41 → 27, сдвиг исчезает, счёт ланов 241 → 240.

**Разброс шага по третям одинаков на всех четырёх сканах: 0.58–0.60%, и всегда
шире последняя треть.** На сгенерированном 0004 шаг ровно 19.0000 по всей
ширине — значит это пара принтер+сканер, а не формат. В пикселях набег один и
тот же, ≈9 px на лист. При шаге 37.6 это четверть лана и ридер переживает; при
18.4 — полулан, и он не переживает. Плотный лист не просто хуже по децибелам,
он ещё и хрупкий к тому, чего на нынешнем шаге не видно вообще.

### 3.5 Две починки, сделанные по итогам (в коде, см. §1)

**Нормировка по пику** (`write_wav`). Сорванные ланы машут в соседей размахом
втрое больше законного, они и задают пик — и все хорошие секунды уезжают вниз.
Именно это слышно как «тихо», и это не свойство листа.

Где резать — выбрали не на глаз. Отношение пика к 99.9-му перцентилю разводит
две популяции втрое, и запас ложится между ними:

| | пик / p99.9 | клипается при p99.9 × 1.3 |
|---|---|---|
| здоровые чтения (01, 02, 03, 05) | 1.19 … 1.46 | 0.000 … 0.001% |
| сам источник (`out3`, `slow3`) | 1.22, 1.23 | 0.000% |
| 04_1, 27 сорванных ланов из 240 | **3.63** | 0.026% |

То есть `HEADROOM = 1.3` не режет ни одного отсчёта здорового файла, включая
исходные wav — это и есть проверка, что режется не музыка, — и режет ровно те
выбросы, которые звуком никогда не были.

**Оценка шага одним числом на лист** (`find_pitch`). Глобальный пик FFT сидит
ниже любой локальной оценки на всех четырёх сканах. Медиана по третям, если в
трети хотя бы восемь ланов; ниже этого бины слишком грубы, а листу такого
размера всё равно негде уползти.

**После обеих** (перечитаны те же пять сканов, `sox speed 2` для медленных):

| лист | tracking, было | стало | rms, было | стало |
|---|---|---|---|---|
| 01 | 11.04 dB | 11.04 | −14.67 dBFS | −15.28 |
| 02 | 7.04 | 7.04 | −15.28 | −15.20 |
| 03 | 12.96 | 12.96 | −14.61 | −15.37 |
| 05 | 14.81 | 14.81 | −15.83 | −14.80 |
| 04_1 | 7.90 (шаг авто) | **8.61** | −22.02 | **−15.33** |

Качество на четырёх здоровых листах не двинулось ни на сотую — починки трогают
только то, что было сломано. 0004 получает даром то, что раньше требовало
`--pitch 18.4642` руками, и приходит на уровне остальных. Источник стоит на
−15.53 dBFS: пять листов теперь укладываются в 0.7 dB вокруг него.

Проверено: `selftest` проходит в обеих версиях, сгенерированные листы читаются
как раньше (0001 и 0002 r=0.9997, 0004 r=0.9972), v2 после тех же двух правок
отдаёт на 04_1 байт-в-байт то же, что корень.

**Переживут ли обе правки другой сканер.** Это было условием, а не удачей: ни
одного измеренного здесь числа в код не попало.

Порог `HEADROOM = 1.3` калиброван не по сканеру, а по звуку — речь дотягивается
до 1.2 своего же 99.9-го перцентиля, и `out3.wav` со `slow3.wav`, которые
стекла не видели, дают те же 1.22–1.23. Другое стекло даст другой процент
сорванных ланов, но решающее отношение (здоровое 1.2 против битого 3.6) не
сдвинется: «сорванный лан машет в соседа во всю ширину лана» — это геометрия
формата, а не железо. Потолок у неё один и не про сканеры: материал, у которого
больше 99.9% времени тишина, а сигнал в коротком всплеске, перцентиль раздавит.
Речи это не касается.

Оценка шага по третям не знает ни величины ухода, ни его знака, ни какая треть
шире. Она знает только «шаг листа не постоянен», а это верно для любой пары
принтер+сканер и различается лишь на сколько. На листе без ухода она возвращает
прежнее число: на сгенерированных страницах сдвиг 0.003 px, а чтения 01, 02, 03
и 05 после правки совпали с прежними побитово. Медиана из трёх вдобавок даром
переживает одну испорченную треть — сканер, дёрнувшийся посреди листа,
отбрасывается сам.

Чего она не чинит: шаг остался одним числом на лист. Сканер, уводящий масштаб
больше чем на полулана по ширине, сломает чтение так же, как ломался 0004, —
просто на широком шаге для этого надо увести вдвое больше. Настоящее решение —
сетка, идущая за бумагой, то есть уточнение границ по каждому лану; см. §4.

### 3.6 Мелочи, замеченные попутно

- «Худшие ланы ниже 0.5» в `bench.py` меряют **паузы в речи**, а не сбои:
  секунды 0, 93, 104, 110, 114 сидят в источнике на −25…−32 dBFS. Считать эту
  строку стоит только по ланам громче порога.
- `prep.bat` против `prep3.bat` на бумаге: 05 (prep) 14.81 dB против 03
  (prep3) 12.92 — те же 60 с, та же сессия, +1.9 dB у более громкого препа.
  Часть этого — везение сканера (рябь 0.93 против 1.37), так что честно
  «около 1–2 dB». Знак совпал с комментарием в `prep3.bat`: ухо предпочитает
  тихий преп, бумага — громкий.
- Временная база на всех листах отличная: ±2 отсчёта, дрейф 1–2 ppm через
  лист. Часы идут верно, промах на 02 — в разрешении, а не в ходе.
- Инструменты этого захода лежат рядом: `score.py` (strict, шире окно лага и
  сразу dB), `align.py` (tracking, карта лага по секундам) и `spec.py` (что
  чтение добавило к спектру того, что печатали — им найден писк из §3.8).

### 3.7 Проверено попутно: v2

`v2/` отличается двумя вещами: ручка `--power` в `paper_sound.py` (мягкая
апертура, по умолчанию 1.0) и защита от субгармоники в `find_pitch`.

На всех пяти реальных сканах v2 отдал **байт-в-байт** то же, что корневая
версия, включая 04_1 — самый плотный лист из напечатанных и единственный
настоящий кандидат на срабатывание защиты. `--power` на этой бумаге не двигает
результат дальше третьего знака (01: 9.10 / 9.11 / 9.09 dB при power 1.0 /
1.5 / 2.0; 03: 13.31 / 13.32 / 13.29), а на power 2.0 худшие ланы даже
проседают, 0.33 → 0.23. Бумага здесь стоит на 244/255 без серого шума — ровно
то, что написано в комментарии к константе.

Вывод: дефолты v2 правильные, обе новинки спят, работать можно на любой из
двух версий. Ручку `--power` держать для скана, пришедшего со стекла зернистым.

### 3.8 Писк на всех чтениях: это полосы сканера

На слух все чтения несут ровный высокий тон. Это не рябь каретки — та даёт 30
циклов на лист, то есть 0.25 Гц, медленное плавание, которое тоном не слышно.

Тон дискретный, **823.4 Гц**, и вот его паспорт (превышение над спектром того
же звука до печати, полоса усреднена по всему листу):

| | частота | превышение |
|---|---|---|
| 0001, только рендер, принтера не видел | — | ничего, ±0.2 dB по всей полосе |
| 01, напечатан и отсканирован | 823.4 Гц | +15.7 dB |
| 02, напечатан и отсканирован | 823.7 Гц | +25.9 dB |
| f_03, f_04_1, f_05 — медленные, подняты ×2 | 1647.1…1647.3 Гц | +18.7…+27.9 dB |

823.4 Гц при sr 6574 — это **ровно 8.00 строк скана на период**. Лист читается
по строке на отсчёт, поэтому любая периодика строк сканера садится прямо в
звуковую полосу чистым тоном. На медленных листах те же 8 строк — уже полотсчёта,
и после обратного ускорения ×2 тон встаёт на 1647 Гц. Он и слышен на `f_04_1`.

**Полосит стекло, а не тонер.** Проверено в пустом поле листа, где чернил нет:

| полоса 60 px без чернил | период 8.00 строк | период 2.79 строк |
|---|---|---|
| 01 | +10.4 dB | +26.9 dB |
| 02 | +8.6 | +25.5 |
| 05 | +10.4 | +23.5 |
| 04_1 | +10.6 | +25.3 |
| 0001 (рендер) | ничего | ничего |

Чистая бумага полосатая — значит это сканер. Периоды совпадают на всех четырёх
сканах до третьего знака (2.787–2.791), включая 05, который сканировался
отдельной сессией. В чернилах линия на 8 строках усиливается до +15…+28 dB:
центроид взвешивает яркостную рябь и переводит её в смещение штриха. Вторая
полоса, 2.79 строки (≈2360 Гц), в звук почти не проходит — она равномерна
поперёк лана, а центроид к равномерной яркости первого порядка глух.

**Убирать почти нечего.** Два способа, оба измерены:

| | 01 | 03 | 04_1 |
|---|---|---|---|
| как есть | 9.10 dB | 12.82 | 6.60 |
| минус общий по всем ланам построчный узор | 8.83 | 12.16 | 6.75 |
| узкий нотч на самом тоне (40q, −30) | **9.39** | 12.68 | **8.11** |

Вычитание общего узора — идея правильная по форме (тон во всех ланах один, звук
в каждом свой), но вычитается заодно общая часть речи, и на здоровых листах это
дороже тона. Нотч окупается там, где тон громкий относительно звука, и вредит
там, где нет.

Вывод: тон занимает узкую щель и по децибелам стоит почти ничего — ухо ловит
чистую синусоиду в шуме куда лучше, чем корреляция. Это вопрос комфорта на
прослушивании, а не починка ридера:

```
sox\sox.exe in.wav out.wav equalizer 823.4 40q -30    rem обычный лист
sox\sox.exe in.wav out.wav equalizer 1647  40q -30    rem медленный, поднятый x2
```

Частота — свойство этого сканера, а не формата. На другом стекле период будет
другой; искать его надо как самый высокий узкий пик в среднем спектре чтения
(`spec.py` рядом это и делает).

---

## 4. Открыто

Порядок — по тому, что чинить раньше.

- **Стенд не видит разницы меньше 2 dB** — §13.1. Два скана одного листа,
  снятые подряд одними настройками, разошлись на 1.95 dB, и выиграл тот, у
  которого рябь каретки хуже, — «сессия была чище» этого не объясняет. Всё,
  что мерилось одним сканом на лист, точнее не знает, и под этот потолок
  целиком попадает C − B = +0.60 dB из §6. Мерить медианой трёх сканов —
  `bench.py --same` (§17.4); переснять C трижды всё ещё некому.
- **Косая резка полос.** Потолок кривизны — один шаг полосы (§16.2), и он в
  одном месте: профиль колонок в `find_tracks` суммируется строго вниз (§18.1).
  Суммировать его вдоль измеренного наклона — порядка двадцати строк и ни
  одного пересчитанного пикселя; потолок тогда упирается в стекло, а не в
  формат, то есть в ~2–3° против нынешних 0.33° (§18.4). Здешние листы стоят на
  0.8 шага в худшем случае, так что это работа не срочная, но и не «другой
  ридер», как здесь стояло раньше.
- **`PILOT_END = 23` не понадобился.** Лестница {1, 2} держит зазор ×1.25 на
  штатных 20 и 32 (§2.3), и запас точности остался себе. N = 4 закрыт вместе с
  ним: он уводит на 30 секунд с листа.
- **`bench.py` меряет одним лагом на лист**, и это по устройству: подорожечная
  привязка по-прежнему уходит в шум чтения, а посекундный поиск живёт в
  `align.py` (разрыв strict/tracking, §3.1 — на 02 он стоил 5.3 dB и один
  неверный вывод). Направление поиска починено 27.08, оно искало не в ту
  сторону и стоило ещё одного неверного вывода (§8.1). Плюс строка «ниже 0.5»
  считает паузы в речи (§3.6). Дефекты стенда, а не формата, но выводы они
  меняли дважды.
- **Часы на узком лане теряют разрешение**: 1–2 отсчёта промаха на лан при
  шаге 18.4 (§3.3). Если плотный лан когда-нибудь понадобится, чинить придётся
  здесь; пока это ещё один довод его не трогать.
- **Сетка не идёт за бумагой.** Медиана по третям (§3.5) сдвинула порог, а не
  сняла его: шаг по-прежнему одно число на лист. Понадобится тому, кто полезет
  плотнее 38.7 px, или тому, чей сканер уводит масштаб сильнее здешних 0.6%.
  Чинить уточнением границ по каждому лану, а не ещё одним делением на части.
- **Писк сканера ничем не давится в самом ридере** (§3.8). Вычитание общего
  построчного узора пробовано и стоит дороже тона; нотч — дело прослушивания,
  и ставить его в `read` без поиска частоты по спектру нельзя, потому что 823 Гц
  — свойство здешнего стекла.
- **`04.png` и механика по часам.** Ридер знает, что часы нашлись на 121-й из
  126, и ничего с этим не делает. (Вторая его беда — двое часов одной частоты —
  закрыта 27.08: лист теперь называет себя, §7.3.1.) После верхнего порога случай не срабатывает,
  но лист с повреждённой крайней полосой его вернёт.

**Закрыто 30.08:** подгонка по полосам строк написана и померена (§16). Сама
по себе она не работает — обрыв не в подгонке, — но поверх наведённого первого
прохода даёт лучшее из всего, что пробовалось. Заодно найден настоящий потолок
кривизны: **шаг полосы**, и он в резке, а не в оценке.

**Закрыто 29.08 (вечер):** тихий отказ оценки перекоса теперь говорит вслух
(§14), и заодно выяснилось, что предел этот в пикселях сдвига, а не в px/строку
— здешние листы стоят к обрыву втрое ближе, чем считалось.

**Закрыто 29.08:** `--rows 2` напечатан и отсканирован своей ручкой, а не
связкой sox (§13): **+2.1 dB** против контроля B по медиане четырёх сканов,
при обещанных §3.2 +1.9. Долг §7.4 погашен, обе README переписаны по этим
числам.

**Закрыто 28.08:** пункт «Известные пределы» в обоих README переписан по
сегодняшним числам — 32.6 dB на цифре против 11.8…12.3 на бумаге, и из двадцати
децибел разницы поимённо названы рябь, перекос, писк 823 Гц, вертикальное
размытие (+1.9 dB, §3.2) и апертура (до 6.0 dB, §6.1). Размытие и апертура
больше не числятся исключениями: обе — статьи расходов с числом.

**Закрыто 27.08 (ночь):** `--rows` собран и лежит в коде (§7); лист говорит
свою скорость сам, часами, и старые листы читаются бит в бит как раньше.

**Закрыто 27.08:** ширина против строк — измерено на бумаге: **+0.6 dB** за
вдвое большую бумагу (§6), то есть шаг 38.7 это левый край плато, а не
вершина. Апертура на бумаге следует шагу, правило §5.1 подтверждено (§6.1).
Стенд, который эти цифры сначала переврал, починен (§8.1).

**Закрыто 26.08:** реальных сканов нового формата теперь пять; авторазворот
перевёрнутого листа — единственное место, где ошибка катастрофична, — сработал
на бумаге 5 раз из 5 (§3). Нормировка выхода и оценка шага починены и лежат в
коде (§1, §3.5).

---

## 5. 27.08: чем мерить, прежде чем мерить

Бумаги в этот день не было. Всё ниже — либо стенд, либо цифровые листы,
и записано потому, что два пункта меняют постановку опытов из §3.

### 5.1 Апертура должна следовать шагу (не внедрено)

`APERTURE = 2·STROKE = 8 px` настроен на шаг 38.7. При удвоенном шаге размах
вдвое больше, штрих проходит за строку вдвое больше пикселей, и окно в 8 px не
вмещает мазок. На цифровых листах (бумаги не касались, обязаны читаться выше
0.999):

| апертура | `sheetB` 38.7 px | `sheetC` 77.4 px |
|---|---|---|
| 8 px | **32.64 dB** | 11.45 dB |
| 12 px | 29.42 | 19.08 |
| 16 px | 26.83 | **31.11** |
| 24 px | 15.86 | 27.44 |

Оптимум едет пропорционально шагу: 8 px при 38.7, 16 при 77.4. Третья точка с
бумаги подтверждает: на `04_1.png` (шаг 18.4) лучшая из измеренных — **4 px**,
0.9082 против 0.9059 у восьми, то есть та же пропорция. На контрольном
`01.png` (шаг 37.58) оптимум 10–12 px, +0.45 dB к умолчанию — паперный шум
сдвигает его чуть вширь.

Правило `narrow = APERTURE · pitch / PITCH` воспроизводит все три и на
существующих сканах не меняет ничего (37.58 → 7.8 px вместо 8). Не внесено,
потому что трогает настроенную константу ридера; до тех пор широкий лист
читать с `--aperture 16` явно.

### 5.2 `04_1.png` три недели сравнивался не с тем файлом

Полушаговый лист (18.4 px, 240 ланов) печатался с **`slow3.wav`** — файла на
половинной скорости, 240 с. Против `04_1.wav` он даёт r = −0.027 и выглядит
как сломанный ридер; против `slow3.wav`:

```
04_1.png vs slow3.wav  r=0.9059  240 ланов, 6569 Гц  медиана 0.9169  ниже 0.5: 27/239
```

Плохие секунды рассыпаны поодиночке по всему листу (1-2, 13, 32, 44, 73, 77,
80, 89 … 237-239), а не собраны в полосу, — значит беда подорожечная, а не
складка и не край стекла. Сегментация на этом листе при этом в порядке: штрих
стоит в центре своего лана (−0.016 шага против −0.022 у здорового `01.png`),
размах тот же 0.069 шага, ланов ровно 240 + двое часов. Точка −4.0 dB из
таблицы §3.3 держится.

### 5.3 Лист C и контроль B: что лежит и как мерить

```
sox\sox.exe out3.wav src59.wav trim 0 59
python paper_sound.py print src59.wav -o sheetC --pitch 77.4
python paper_sound.py print src59.wav -o sheetB --pitch 38.7
```

59 секунд, а не 60 из §3.3: при 77.4 px лист держит ровно 59 плюс двое часов,
шестидесятая уезжала на второй лист.

**Контроль печатать вместе с опытом.** В §3.1 разница между листами оказалась
сканером, а не форматом; сравнивать `sheetC` с точкой 0 dB, снятой 26.08 на
другой сессии, — это мерить ещё и день. `sheetB` несёт тот же звук на штатном
шаге, и отличается от `sheetC` только шагом.

Опорные точки до бумаги: **B 32.64 dB при апертуре 8, C 31.11 dB при 16.**
После скана:

```
python bench.py scanB.png src59.wav
python paper_sound.py read scanC.png --aperture 16 -o rC.wav
python score.py rC.wav:src59.wav
```

Ответ на §3.3 — разность C − B на бумаге. Экстраполяция обещала ≈ +3.1 dB
против +1.9 у `--rows 2`.

### 5.4 Потолки резака, измеренные

`cut_lanes.py` больше не врёт папкой (чистит её перед резом), логом (писал
`.tif`, когда писал `.png`), молчанием про часы (ноль и один час теперь
говорят; поворот по-прежнему требует двух в обратном порядке — на листе без
часов обычный лан прошёл тест `pilot_bin` и дал `[None, 20]`, одной такой
улики хватило бы, чтобы перевернуть весь лист) и полоской, которую не удалось
навести (её обрезают и растягивают, как остальные, и кричат номером).

Измеренные границы, вписанные в `ponytail:`-комментарий к `BLEED`:

| что | где ломается |
|---|---|
| косина листа | до 17.6 px безразлична (r 1.00000); расширение кропа по трассе не меняет ни цифры |
| она же дальше | с ~30 px лан гуляет шире собственного шага, вертикальные резы не отделяют его от соседа: 0.37 |
| скорость штриха | при 0.9 размаха и частоте в шестую долю рейта штрих идёт 28 px за 7 строк, строка — мазок: 0.11…0.57 в полосках против 0.70…0.89 в wav-чтении той же страницы |

Последнее — предел носителя, а не реза: его видят оба ридера. На A4@600 это
около 1 кГц при 90% экскурсии, куда `prep*.bat` с крестом 5…7.5 не приходит.

Selftest резака расширен до того, что раньше только утверждалось: png
действительно пишется и сравнивается с tif, косой лист режется через `cut()`
(с наклоном, снятым с обеих сторон — иначе честный лист читается 0.73),
громкий режется через `cut()`, а лист на полной экскурсии проверяется на то,
что он **предупреждает**: при 1.0 сетка встаёт на полшага, крайние ланы
приходят половинками (17 и 18 px весом 0.476 и 0.543 медианы), и это записано
как потолок, а не как счёт.


---

## 6. 27.08, вечер: лист C на бумаге (пересчитано ночью)

> **Первая редакция этого раздела была неверна.** В ней стояло «C проигрывает
> B 5.1 dB». Это оказалось не бумагой, а стендом: `score.py` и `bench.py`
> искали общий лаг только в одну сторону, а бумажное чтение опаздывает на
> отсчёт-другой, то есть ровно в другую. Ошибка стоила B 4.8 dB, а C — 10.4,
> и вся разность между ними была почти целиком ею. Разбор — §8.1. Ниже цифры
> уже с двусторонним поиском.

Оба листа напечатаны и отсканированы подряд, одной сессией, одним стеклом —
как требует §5.3. Оба легли вверх ногами, оба развёрнуты автоматически (теперь
7 из 7), по двое часов, рябь каретки 1.26 и 1.39 отсчёта rms.

| лист | шаг | размах | апертура | до бумаги | на бумаге | медиана |
|---|---|---|---|---|---|---|
| B (контроль) | 38.7 px | 15.9 px | 8 | 32.64 dB | **11.76 dB** (r 0.9683) | 0.9684 |
| C | 77.4 px | 35.2 px | 20 | 31.11 dB | **12.36 dB** (r 0.9722) | 0.9725 |

**C − B = +0.60 dB.** Обещано было +3.1 (§3.3): знак верный, величина впятеро
меньше. Удвоение шага окупается, но так слабо, что платить за него половиной
секунд на листе незачем.

Практический вывод §3.3 не изменился — шаг 38.7 остаётся рабочим, — но причина
у него теперь другая. Не «кривая за коленом и вправо падает», а **кривая
вправо почти горизонтальна**: влево круто вниз (−4.0 dB при половине шага,
§3.2, снято через `align.py`, то есть двусторонним поиском, и потому уцелело),
вправо +0.6 за вдвое большую бумагу. 38.7 — левый край плато, а не вершина.

### 6.1 Апертура на бумаге следует шагу: правило §5.1 подтвердилось

| апертура | 4 | 6 | 8 | 10 | 12 | 16 | 20 | 24 | 32 |
|---|---|---|---|---|---|---|---|---|---|
| B (38.7) | 8.53 | 11.52 | **11.76** | | 11.74 | 11.53 | | | |
| C (77.4) | | | 6.39 | 9.38 | 11.20 | 12.25 | **12.36** | 12.36 | 12.00 |

Оптимум едет с шагом ровно так, как обещала §5.1: 8 px при 38.7, 16–24 при
77.4 (правило `narrow = APERTURE · pitch / PITCH` даёт 16, бумага сдвигает чуть
вширь — как и на контрольном `01.png`). Читать широкий лист умолчанием стоит
**6.0 dB**. Это уже не гигиена, а поправка, которой место в коде.

Прошлая редакция утверждала обратное — «на бумаге апертура плоская». Плоской
она была потому, что при одностороннем лаге всё тонуло в ошибке привязки:
стенд мерил не окно ридера, а собственный промах.

### 6.2 Куда пошёл размах

- **Писк сканера размах победил.** Полоса 823 Гц (§3.8) стоит на C **+8.8 dB**
  над источником против **+16.6** на B. Против шума фиксированной амплитуды
  удвоение размаха дало ровно то, что обещало — вдвое лучшее отношение.
  (`spec.py` считает спектры мощности и к лагу слеп, так что эта цифра
  единственная из §6, которая не менялась.)
- **Крутизна съедает почти весь выигрыш.** `slope.py`: ошибка растёт с
  крутизной штриха на обоих листах, и C лучше B в каждом бине, но с разным
  запасом — −13.2 против −11.1 dB на пологом и −4.7 против −4.5 на почти
  вертикальном. При удвоенном размахе штрих проходит вдвое больше пикселей за
  строку, потолок носителя (§5.4) ближе, и он забирает почти всё, что дал
  размах. Остаток и есть те 0.6 dB.
- **По ланам ровно** у обоих: первая половина листа 0.967 / 0.970, вторая
  0.970 / 0.975. Ни полосы, ни края стекла.

### 6.3 Что из этого следует

- Шаг 38.7 — **левый край плато**, а не максимум: уплотнять нельзя (−4.0),
  расширять незачем (+0.6 за вдвое большую бумагу).
- `--rows 2` с его +1.9 dB (§3.2, снято двусторонним поиском) остаётся
  единственной ручкой, которая заметно платит за бумагу. Лист D её проверит.
- **Апертура привязана к шагу** (§6.1) — внесено, см. §9.

## 7. 27.08, ночь: `--rows` собран

- `print --rows {1,2}` — отсчёт получает N строк бумаги. Лан по-прежнему
  `rate` строк; звук просто растягивается перед разметкой, и `lay_out`,
  `render_page` и вся геометрия об этом не знают ничего.
- **Лист несёт N сам.** Часы печатаются с периодом в отсчётах (32 и 20), на
  бумаге он выходит в N раз длиннее, и `pilot_bin` выбирает ближайшего в
  логарифме из лестницы **20, 32, 40, 64 строки**. У `read` флага нет.
- Допуск на часы был «октава в любую сторону», стал `PILOT_SLACK = 1.118` —
  половина ближайшего зазора лестницы (×1.25). Реальные сканы приходят с 3.1%,
  стенд с 0.1%, запас вчетверо; окно ложных часов при этом стало **уже**
  прежнего, а не шире.
- Прореживание стоит **после** часов (их читают в полный рост) и режет по
  ланам, так что лан остаётся целым числом отсчётов при любом sr.
- Оба клока обязаны сойтись в N, иначе лист читается как N=1: ошибка здесь так
  же катастрофична, как незамеченный переворот, — половина отсчётов и двойная
  скорость.

Регрессия: `sheetB_scan` и `sheetC_scan` перечитаны **бит в бит** теми же
файлами; 01, 02, 03, 04, 04_1 и 05 по-прежнему находят по двое часов и
разворачиваются как раньше; `cut_lanes --selftest` цел. Selftest дополнен
листом на две строки, лицом и вверх ногами.

### 7.1 Два места, где связка теряла децибелы

Цифровой лист, бумаги не касался — это потолок, о который упрётся любое
измерение на бумаге:

| путь | цифровой потолок |
|---|---|
| `--rows 1` — контроль | 32.64 dB, не сдвинулся |
| `--rows 2`, как получилось сначала | 15.97 |
| `--rows 2`, после двух починок | **25.07** |

- **Сетка растяжки.** `to_rate` кладёт точки от края до края: при удвоении это
  скорость (2n−1)/(n−1), а не 2, и за лист набегает полстроки дрейфа, которую
  точное прореживание вернуть не может. Растяжка теперь на своей сетке,
  `np.interp(arange(n·rows) / rows)`, где каждый исходный отсчёт лежит **на
  строке**, и прореживание возвращает его нетронутым.
- **Фильтр прореживания.** Бокс из `to_rate` (k=3) — 15.9 dB, окно-синк —
  25.1. Вся разница в завале полосы: бокс роняет верх на 3.5 dB. Длина окна не
  важна, 8 и 32 полувитка дают 24.1 и 24.5, — потолок ставит прямая, которой
  `print` растягивает, а не скат фильтра.

Фильтр тут не деталь прореживания, а сама суть `--rows 2`: на двух строках
звук занимает нижнюю половину полосы листа, верхняя — чистый скан, и деление
без фильтра складывает её обратно в звук. То есть возвращает ровно тот шум,
ради ухода от которого потрачено полбумаги.

25 dB против семи на бумаге (§6) — связка стоит около **0.1 dB** результата.
Мерить больше не мешает.

### 7.2 Апертура на двух строках

На маленьком листе selftest (600 Гц, 8 ланов) апертура 8 px при `--rows 2`
стоила 16 dB против 4 px: штрих идёт вдвое положе, мазок в строке компактнее,
и широкое окно прихватывает соседа. **На настоящем листе этого нет** — 4/6/8
px дают 24.9/25.4/25.1 dB, проваливается только 12. Умолчание трогать не надо;
синтетика ведёт себя иначе потому, что её лан подолгу стоит в упоре, а речь
так не делает.

### 7.3 Что бывает, когда ридер не угадал

Промах несимметричен, и обе стороны измерены на `sheetD`:

| промах | что выходит |
|---|---|
| две строки приняты за одну | 120 с вместо 59, октавой ниже. Поднято руками (`[::2]`): **29.44 dB** — выше штатных 25.07, потому что ручное деление минует антиалиасный фильтр, а цифровому листу тот и не нужен. На бумаге будет наоборот |
| одна строка принята за две | 32.64 → **12.49 dB**. Выброшена верхняя половина полосы, из wav не вернуть |

Поэтому решение спрашивает **обоих** клоков. Ошибка масштаба ×2, нужная чтобы
клок перепрыгнул ступень, физически невозможна (сканер даёт 3%), так что
единственный реальный путь ко второй строке таблицы — **ложные часы**: лан,
чья трасса достаточно похожа на тон. Прецедент есть (§5.4). Одиночный клок
теперь не прорежает, а читает как 1 и печатает, каким флагом это поправить:
дешёвая ошибка вместо дорогой.

Флаг — `read --rows N`. Он же единственный способ прочитать лист на двух
строках с побитыми часами: больше это число на листе не написано нигде.
Прорежающий фильтр вынесен в `thin()`, потому что теперь его зовут и с путей,
где часов нет вовсе.

### 7.3.1 Заодно: лист с двумя одинаковыми часами теперь говорит

`04.png` напечатан до того, как часы разошлись по частотам: на обоих концах
32 отсчёта на цикл. Ридер был прав, что не разворачивал, — лист симметричен, и
сказать ему нечем, — но **молчал**, а `cut_lanes` про этот случай говорил ещё
с самого начала («worth saying out loud rather than treating as a quiet no»).
Теперь говорит и `read`. На бумаге: 0.0029 как лежал, **0.4706** с
`--upside-down` (низко не из-за разворота — это старый скан с рябью 2.33).

Это единственный случай, когда лист играет задом наперёд и **ни одно измерение
не сообщает о неисправности**, — поэтому предупреждение, а не догадка. На
листах 01, 05, `sheetB_scan` и `sheetD` не срабатывает: у них 20 и 32.

### 7.4 Чего ещё не было

Бумаги на `--rows 2` этой ручкой не было: +1.9 dB из §3.2 сняты связкой sox.
Печатать по протоколу §5.3, вместе с контролем (`sheetD.png` лежит, `sheetB.png` перепечатать тот же файл — он бит в бит прежний):

```
python paper_sound.py print src59.wav -o sheetD --rows 2
python bench.py sheetD_scan.png src59.wav
```

При шаге 38.7 и двух строках лист держит ровно 59 с — **та же бумага на
секунду, что у `sheetC`**, потраченная другой ручкой. Тогда все три точки §3.3
окажутся сняты на одном звуке: B (полбумаги), C (вся бумага в ширину, −5.1 dB)
и D (вся бумага в строки, обещано +1.9).

Напечатано 28.08, считано 29.08: **§13**. Обещание сошлось — +2.1 dB, — а
заодно выяснилось, чего стоит один скан на лист (§13.1).

---

## 8. 27.08, ночь: стенд искал лаг не в ту сторону

Всплыло сбоку: сравнивали чтение ридера с `picky.py` на одних и тех же
полосках, и метрику для сравнения пришлось писать заново — с поиском смещения
по всей длине, потому что picky кладёт в начало 3000 отсчётов тишины и целую
секунду головных часов. Новая метрика сказала про `rC.wav` **0.9712**, а
`score.py` про тот же файл — **0.7717**.

### 8.1 Ошибка

`bench.score` и `score.py` брали общий лаг так:

```python
rs = [np.corrcoef(a, b[d:d + n])[0, 1] for d in range(lag + 1)]
```

`a` — чтение, `b` — источник. Сдвигается **источник, вперёд**, то есть
поправка умеет чинить только чтение, которое пришло **раньше** файла. А
шапка самого `bench.py` описывает ровно обратный случай, и правильно:

> «Print and scan fade the ink's first row into the paper, so a read sits a
> sample or two **late** against the file uniformly… on broadband audio it is
> worth 0.15 of correlation.»

Опоздание чинится сдвигом **чтения**, то есть `d < 0`, а в `range(lag + 1)`
таких нет. Код делал противоположное тому, что написано в его же шапке, и
записывал ошибку привязки в шум бумаги. Это второй раз, когда один общий лаг
подсовывает неверный вывод, — первый разобран в §3.1, там же и предупреждение,
которое не спасло.

Проверка, что это не подгонка: на **цифровых** листах оптимум ровно 0 и в обе
стороны, потому что там нечему растекаться. Поправка — один целый сдвиг на
59 секунд, один параметр.

### 8.2 Что пересчиталось

| чтение | было (лаг ≥ 0) | стало (лаг обе стороны) | лаг |
|---|---|---|---|
| `digq` — цифровой лист | 32.64 dB | 32.64 dB | 0 |
| `01.png` | 9.10 | 9.10 | 0 |
| `04_1.png` | 0.9059 r | 0.9190 r | +1 |
| `02` | 3.45 dB | 7.01 | +1 |
| **`sheetB_scan`** | **6.96 dB** | **11.76** | +1 |
| **`sheetC_scan`** | **1.84 dB** | **12.36** | +2 |

Задет ровно тот случай, в котором листы различались: B опаздывал на отсчёт, C
на два, и разность лагов стала «разностью форматов» в 5 dB. Ноль на цифровых
листах и на `01` объясняет, почему это прожило три недели.

Починено в `bench.py`, `score.py` и `slope.py`; `align.py` был прав всегда —
он ищет в окне ±3 с в обе стороны, и потому колонка tracking в §3.2 уцелела,
а вместе с ней +1.9 dB за `--rows 2` и −4.0 dB за половинный шаг.

### 8.3 picky против ридера, на одних и тех же полосках

Одна метрика для обоих: посекундный r против того же источника, смещение
найдено по всей длине.

| лист | ридер | picky `x 50 10 d 330 1 0.95 pdp 6500 24` |
|---|---|---|
| `01_lanes` против `out3` | **0.9499** (9.65 dB) | 0.8652 (4.74 dB) |
| `sheetC_scan_lanes` против `src59` | **0.9712** (12.20 dB) | 0.7932 (2.29 dB) |

Разница 4.9 и 9.9 dB. Что её делает, по убыванию:

- **Режим `50` нормирует каждую полоску отдельно** (`per_mode = 0`, `scale_vec`
  берёт min/max своей полоски). Тихая секунда подтягивается к громкой:
  динамика сплющена, на стыках «дыхание» с периодом в лан. Ридер нормирует
  один раз на файл. Лечится режимом `51`.
- **Фильтр 20 Гц взведён дважды** (`330 = 2·3·5·11`: и на полоску, и на
  склейку) — два каскадных Butterworth 2-го порядка, причинных, и на каждой
  полоске он стартует с нуля, то есть в начале каждой секунды переходный
  процесс. У ридера одно вычитание скользящего среднего в 331 отсчёт: 8.8 Гц,
  линейная фаза, без перезапусков.
- **Степень 10** — это не центроид, а мягкий argmax по ядру штриха. Зерно
  бумаги выключено (тишина чище), но измерение перестаёт быть слепым к
  плотности, а это слепота намеренная; и на крутых участках степень тянется за
  тёмным ядром, а не за центром массы чернил.
- **Нет ретайминга:** рябь каретки (1.16 и 1.39 отсчёта rms) остаётся как вау.
- **Окно во всю ширину полоски** против 8 px у ридера: на `01` подхватывается
  сосед, на C — 79 px бумаги вокруг 4 px штриха.
- Часы остались в пачке (секунда тона с каждого конца) и объявленные 6500 Гц
  против 6574/6579 (весь файл на 1.2% ниже, около 20 центов).

Ридер выигрывает у picky и там, где сам ошибался: даже со сломанным стендом
он читал те же чернила лучше. Но услышанная разница — настоящая, и половина её
приходится на два ключа в командной строке (`50` вместо `51` и двойной 20 Гц),
а не на алгоритм.


---

## 9. 27.08: окно ридера следует шагу (внесено)

`narrow = APERTURE · pitch / PITCH`, где шаг — тот, что уже измерен с листа
(`span` в `read_curves_page`). Флаг `--aperture` теперь означает «столько px и
ни пикселем больше»: умолчание масштабируется, заданное число — нет.

Что стало со всеми имеющимися сканами, умолчанием и без флагов:

| лист | шаг | было | стало |
|---|---|---|---|
| `sheetB_scan` | 37.4 | 0.9683 | 0.9682 |
| `01.png` | 37.6 | 0.9436 | 0.9435 |
| `04_1.png` | 18.4 | 0.9190 | **0.9216** |
| `sheetC_scan` | 76.9 | 0.9018 | **0.9711** |

Широкий лист поднялся на 6.0 dB и больше не требует флага руками; полушаговый
выиграл вторым знаком (окно 3.8 px против 8 — §5.1 мерила там оптимум 4);
штатные листы не сдвинулись дальше четвёртого знака, как правило и обещало.

То же правило внесено в `cut_lanes`: в `clock_at` — окно, которым читаются
часы, а это то самое измерение, чей отказ переворачивает лист, — и в маску
полосок. Маска в 8 px на листе с шагом 77.4 срезала штрих, который была
обязана сохранить: лане-широкое чтение нарезанных полосок C поднялось с
**0.7194 до 0.7686** (+1.3 dB) просто оттого, что маска перестала резать.


---

## 10. 28.08: `cut_lanes` читал часы вертикально вниз

`clocks()` звал `lane_centroid` с `drift=0.0`, тогда как ридер каждую полосу
читает вдоль скива, снятого `lane_drift()`. На `sheetB_scan` это стоило вторых
часов, а вторые часы — единственное, на чём резак разворачивает лист: он
находил одни, честно говорил «одних мало», оставлял лист как лежал, и полоски
выходили задом наперёд. Молча — потому что резак не сравнивает ни с чем.

Изолировано по четырём вариантам, окно × скив:

| лист | наклон листа | окно | вниз | вдоль скива |
|---|---|---|---|---|
| `sheetB_scan` | **−32.1 px** | 7.65 px | `[20, None]` | **`[20, 32]`** |
| `sheetC_scan` | −36.4 px | 15.50 px | `[20, 32]` | `[20, 32]` |
| `01.png` | −4.1 px | 7.86 px | `[20, 32]` | `[20, 32]` |

Ширина окна тут ни при чём — обе колонки одинаковы при 8 px и при
масштабированном. Дело в наклоне: `sheetB_scan` — самый косой лист в наборе, и
окно в 7.65 px уходит с его штриха. C наклонён даже сильнее, но его окно вдвое
шире и той же прогулки не замечает; `01` лежал почти ровно. Совпадение трёх
условий — узкое окно, сильный наклон, — поэтому и не всплывало.

Починено: `clocks()` меряет скив сам (`lane_drift`, тот же вызов, что и
`traces()`) и передаёт в `clock_at`. Цена — лишний первый проход по странице на
лист, и это правильная цена за измерение, чей отказ переворачивает звук. Теперь
все четыре листа ведут себя как у ридера: B, C и `01` разворачиваются сами,
`04` говорит, что сказать не может.

**Регрессионного теста на это нет, и вот почему.** Синтетика его не
воспроизводит: на чистых чернилах липкое окно (`STICKY = 11`) само отслеживает
наклон до самого места, где умирает сегментация (проверено на 400, 1200, 2000
и 3000 строк при наклоне 16…40 px), и добавленный шум до σ=60 ничего не меняет
— часы читаются вниз до последнего. Ломается только настоящая бумага, где
липкое окно борется с наклоном и со сканом одновременно. Улика — сам файл
`sheetB_scan.png`, и она записана здесь.

---

## 11. 28.08: разбор ревью

### 11.1 Две дырки, внесённые вместе с масштабируемым окном

Обе — цена того, что `--aperture` стал `None` вместо числа, и обе мои.

- `cut_lanes` перестал печатать частоту на дефолтном пути: `if args.aperture:`,
  а `None` и `0` одинаково ложны. Стало `!= 0` — ноль по-прежнему значит «маски
  нет, полоска высотой в страницу», `None` значит «окно по шагу», и это как раз
  тот случай, когда полоска и есть лан.
- `np.median([])` на пустом `find_tracks` стрелял **раньше**, чем
  `if not lanes` успевал сказать, что листа нет. Переставлено.

### 11.2 Полоски с `--rows 2` теперь называют свою скорость

`clock_at` выбрасывал третий элемент `pilot_bin` — как раз `rows`. Теперь
`clocks()` возвращает `(частоты, строк-на-отсчёт)`, и на тонком листе резак
говорит прямо, что полоска — **полсекунды**, а её ряды это ряды бумаги. Плюс
строка про частоту стала честной:

```
sheetD.png: the clocks say two rows to the sample -- a strip here is HALF a
second, and its rows are the paper's rows...
  one strip is 6780 rows and holds 1/2 of a second, so the audio on it is 13560 Hz
```

Прореживать саму картинку не стали: полоска обещает, что её ряд — ряд бумаги,
и это обещание дороже удобства. Ревью предлагало то же.

### 11.3 Заполнение дыры получило тест

Было измерено и записано в комментарий, но не проверялось ничем. Теперь в
selftest: лист на 11 ланов, пятый стёрт, и проверяется не только длина, а
**что хвост не поехал** — лан после дыры коррелирует со своей секундой и не
коррелирует со следующей, то есть ровно та пара, которая различает «заполнено»
и «выброшено». Проверено, что тест кусается: с отключённой вставкой selftest
падает на `a wiped lane left 10 lanes of 11`.

### 11.4 Два вывода ревью, которые не подтвердились

- **«`to_rate` на печати складывает 44.1 → 6.7 кГц в полосу, box стоит
  11.5 dB».** 11.5 dB было измерено для прореживания ровно вдвое, где box
  — три отвода. При 44100 → 6780 он семь отводов, и померено это так:
  **−2.4 dB на 2.5 кГц и +1.2 dB заворота выше 3.3 кГц.** Ощутимо, но не
  катастрофа, и в этом конвейере не срабатывает вовсе: `prep3.bat` отдаёт
  `rate -v 6780`, то есть `to_rate` на печати — тождество. Чинить сам `to_rate`
  нельзя дёшево: им меряются обе стороны каждой оценки. Поэтому `print` теперь
  **говорит**, когда частота входа не равна листу, и называет команду sox.
- **«фильтр `thin` лучше делать по дорожке, а не по склейке».** Померено:
  **25.04 dB против 25.04.** Разницы нет. И по существу склейка — не граница:
  секунды идут подряд, звук за стыком продолжается, а фильтр по дорожке
  выдумал бы край, которого на бумаге нет, и заваливал бы последние отсчёты
  каждой секунды к нулю. Докстринг переписан, чтобы не читался как «стыка
  фильтр не касается»: касается, на восемь отсчётов в каждую сторону, и так
  и надо.

### 11.5 Принято к сведению, не сделано

Канон кадра в `traces()` (якорь левой дорожки, `y`, `a0 + span/2`) по-прежнему
скопирован из `lane_centroid`. Сегодня не баг — обе копии совпадают, — но это
та же мина, что уже сработала однажды на скиве и на предупреждении об узкой
дорожке (§1). Правильное лечение — `absolute="page"`, возвращающий `(rows, col)`
из одного места. Отложено: тронуть придётся оба ридера сразу, а сейчас на
столе бумага.

---

## 12. 28.08: правки из `fix/` внесены

Взяты как база, поверх — одна правка. Обе исходные оценивались по бумаге, а не
по виду.

### 12.1 `absolute` отдаёт кадр страницы — взято как есть

`lane_centroid(..., absolute=True)` возвращает `(rows, колонка на странице)`,
и `traces()` схлопнулась с восемнадцати строк до трёх. Это долг §11.5,
погашенный в правильном месте: кадр (переписанный `x0` обрезанного крайнего
лана, `reach`, кроп под `ink_rows`, среднестраничный `y`) жил в двух файлах
буквально одинаковыми выражениями, теперь в одном.

Проверено не на глаз: **61 полоска из 61 совпала байт в байт** на обоих
реальных листах, `sheetB_scan` (32 px наклона — где расхождение кадров вылезло
бы первым) и `sheetC_scan`.

### 12.2 `to_rate`: box → синк, но длина ядра по отношению

Идея верна, и моё возражение против неё не подтвердилось: этим ресемплером
меряются обе стороны каждой оценки, но подмена стоила **+0.02 dB** (rB 11.76 →
11.78, rC 12.25 → 12.26). Журнал переписывать не пришлось.

Ядро при этом стояло фиксированным — 33 отвода при любом отношении, — и это
половина дела. Померено против ресемплера в 513 отводов, путь печати
44.1 кГц → 6780:

| | 1.0k | 2.0k | 2.5k | 3.0k | 3.3k |
|---|---|---|---|---|---|
| box (было) | −0.4 | −1.5 | −2.3 | −1.8 | **+0.6** |
| синк, 33 отвода | −0.0 | −1.0 | −2.3 | **−3.1** | −0.9 |
| синк по отношению | −0.0 | +0.0 | +0.0 | **−0.2** | −0.8 |

При отношении 6.5× тридцать три отвода — это два с половиной периода среза,
то есть фильтра почти нет: заворот он убирает, но роняет 3 кГц сильнее, чем
box, который заменил. Обратная сторона той же константы: при мягком отношении
(6780 → 6575, путь оценки) те же 33 отвода дают наоборот слишком резкий срез
там, где box не делал ничего, — −2.6 dB на 3.2 кГц по опорному сигналу.

Поэтому `DEC_TAPS` теперь считается **в периодах среза, а не в отсчётах**, и
`lowpass()` переводит их в длину: `t = ceil(cycles / ratio)`. Одна функция на
двоих, `to_rate` зовёт её с `rate/sr`, `thin` — с `1/rows`. Стоит 0.32 с
против 0.16 на файле в 5.3 млн отсчётов, то есть даром.

### 12.3 Полный круг

| | до `fix/` | после |
|---|---|---|
| цифровой лист, `--rows 1` | 32.64 dB | 32.64 |
| цифровой лист, `--rows 2` | 25.07 | **25.10** |
| `sheetB_scan` | 0.9682 | 0.9683 |
| `sheetC_scan` | 0.9711 | **0.9712**, и ниже 0.5 стало 0/58 вместо 1/58 |
| `01.png` | 0.9435 | 0.9436 |
| `02.png` | 0.9124 | 0.9124 |
| `04_1.png` | 0.9216 | 0.9216 |
| `sheetD.png` | 0.9981 | 0.9981 |
| полоски B и C | — | 61/61 байт в байт |

Оба selftest'а зелёные, `align`, `slope`, `spec`, `score`, `cmp_picky` бегают
и дают те же числа до второго знака. Ничего не потеряно, две цифры подросли.

---

## 13. 29.08: лист D на бумаге — `--rows 2` своей ручкой

Долг §7.4 и первый пункт §4. Лист напечатан 28.08 по протоколу §5.3
(`print src59.wav -o sheetD --rows 2`, шаг 38.7, 59 секунд на A4 — та же
бумага на секунду, что у C), отсканирован четырежды в тот же вечер и до
сегодня не считан.

**Скорость лист назвал сам, все четыре раза.** Каждый скан пришёл как
118 ланов, двое часов и `2 rows a sample = 0.5 s a lane`; `--rows` ридеру не
передавался ни разу. Механика §7 на бумаге работает.

| скан | как снят | рябь, отсч. rms | r | dB | медиана по секундам |
|---|---|---|---|---|---|
| D1 | 602 dpi, палитра | 1.09 | 0.9789 | 13.61 | 0.9760 |
| D2 | 602 dpi, палитра | 1.20 | 0.9811 | 14.10 | 0.9818 |
| D3 | 600 dpi, RGB | 1.36 | 0.9757 | 12.98 | 0.9733 |
| D4 | 600 dpi, RGB | 1.75 | 0.9843 | **14.93** | 0.9842 |
| **B** (контроль, 27.08) | 600 dpi | 1.26 | 0.9683 | 11.78 | 0.9684 |
| C (27.08) | 600 dpi | 1.39 | 0.9716 | 12.26 | 0.9713 |

Медиана четырёх сканов D — **13.86 dB**, то есть **+2.1 dB против B**.
Обещано было +1.9, снятые связкой sox (§3.2). Сошлось.

Tracking (`align.py`) говорит то же самое: D4 0.9846 против B 0.9690, разрыв
strict/tracking на штатном шаге по-прежнему ничтожен (§3.1) — привязка ланов
к отсчётам не врёт, и мерить можно любой из двух метрик.

D2 лёг на стекло вверх ногами и развернулся сам: **8 из 8**.

**Цифра и бумага расходятся в разные стороны, и это одно и то же явление.**
На цифровом листе `--rows 2` **хуже** контроля на 7.5 dB (25.10 против 32.64,
§12.3) — вдвое меньше строк на секунду, вдвое меньше разрешения по времени. На
бумаге он **лучше** на 2.1. Разница между этими числами и есть вертикальное
размытие печати и скана: на цифре душить нечего, а на бумаге оно съедает
больше, чем стоит отданное разрешение.

### 13.1 Разброс между сканами одного листа больше, чем весь эффект §6

D3 и D4 — один лист, одно стекло, две минуты друг от друга, одни настройки:
**12.98 и 14.93**. Почти два децибела на ровном месте. Причём выиграл тот, у
которого рябь каретки **хуже** (1.75 против 1.36 отсчёта rms), так что
«сессия была чище» этого не объясняет, и ретайминг по часам, видимо, рябь
такого размера добирает целиком.

Это потолок стенду, а не находка про лист: **разность двух листов, снятая по
одному скану каждого, ничего не значит ниже примерно 2 dB.** Под потолок
целиком попадает **C − B = +0.60 dB** из §6 — величина меньше разброса, знак
может оказаться и обратным. Вывод §6.3 («38.7 — левый край плато») от этого не
рушится: он держится на −4.0 dB влево, снятых по пяти листам через `align.py`.
Но саму цифру +0.6 надо считать неизмеренной.

Не попадает под потолок ничто из решающего: +2.1 у `--rows 2` (четыре скана
против одного, разница больше разброса) и −4.0 у половинного шага.

Чинить медианой трёх сканов на лист, а не ещё одним листом: скан стоит две
минуты, печать — лист бумаги и сессию.

### 13.2 Три точки §3.3, снятые на одном звуке

| точка | бумаги на секунду | на бумаге |
|---|---|---|
| B — штатный лист | 1 | 11.78 dB |
| C — вся вширь (шаг 77.4) | 2 | 12.26 (±2, §13.1) |
| D — вся в строки (`--rows 2`) | 2 | 13.86 (медиана 4 сканов) |

При равной бумаге на секунду строки идут впереди ширины на 1.6 dB — **это
разворот вывода §3.3**, где ширина обходила строки на 2.4 dB. Разворот
объясним: та оценка снята с листа 0004, у которого `--rows 2` стоял на
половинном шаге 18.4, где и часы теряют разрешение (§4), и сам лан стоит
−4.0. §3.3 мерила уплотнение лана, а не размен «ширина против строк».

Честно про величину: 1.6 dB меньше разброса §13.1, а C снят одним сканом.
Это направление, а не число. Практический вывод не меняется ни при каком
знаке — за лишнюю бумагу платят `--rows 2`, а не шагом, потому что шаг ещё и
роняет разрешение часов.

### 13.3 Мелочи

- **D1 и D2 несут `pHYs` с неверным CRC** — сканер записал 602 dpi и не
  пересчитал контрольную сумму. `read` чинит чанк в памяти и говорит об этом,
  файл на диске не трогает; голый Pillow эти два файла не открывает вообще
  (`UnidentifiedImageError`). Оба, кроме того, помечены правкой уровней
  (87 пустых корзин) — и всё равно читаются на 13.6 и 14.1.
- Одна секунда ниже 0.5 у всех четырёх сканов, и всегда первая: это пауза в
  начале `src59.wav` (§3.6), а не дефект листа.

---

## 14. 29.08, вечер: обрыв оценки перекоса — в пикселях, и лист о нём говорит

Последний катастрофический отказ, который не сообщал ничего: одна прямая,
подогнанная по среднему центроидов, после какого-то перекоса схлопывается в
ноль, чтение уходит на r ≈ 0.1, а сегментация, часы, число полос и частота при
этом сходятся. Разбирался, где именно обрыв, чтобы понять, стоит ли он ручки.

### 14.1 Предел меряется в пикселях сдвига, а не в px на строку

Все прежние числа (точна до 0.04, занижает на 0.06, схлопывается на 0.08) сняты
на листах в 600 строк, и в README перенесены как px/строку. Это неверно: на
листе в 6570 строк 0.06 px/строку — это 394 px, четверть ширины листа. Прогнал
одинаковый лист на трёх высотах:

| сдвиг поперёк листа | 600 строк | 2000 строк | 6570 строк | чтение |
|---|---|---|---|---|
| 24 px | точно | точно | точно | r 1.00 |
| 36 px | −8% | −9% | −11% | r 0.99 |
| 48 px | ноль | ноль | ноль | **r 0.02…0.39** |
| 80 px | ноль | ноль | ноль | r ≈ 0.1 |

Обрыв стоит на месте в пикселях сдвига при любой высоте листа — механизм в
том, что штрих уходит из окна апертуры, а окно живёт в пикселях.

**Где стоят здешние листы:** 01 — 4 px, 02 — 6 px, 03 — 3 px, 04 — 9 px,
04_1 — 8 px, 05 — 5 px, `sheetD_scan4` — 23 px, `sheetD_scan2` — 28 px,
`sheetD_scan3` — 30 px, `sheetB_scan` — 31 px, `sheetC_scan` — 36 px. То есть
кривые листы прошли **треть пути** от «точно» до «схлопнулось», и `sheetC_scan`
уже стоит на 36 px, где оценка занижает на десятую долю. Прежняя формулировка
«запас примерно двукратный» сравнивала 17.7 px с 24 px и была права случайно.

### 14.2 Две проверки, которые не заработали

- **Развал прямой по половинам листа.** Подогнать её отдельно по верхней и
  нижней половине строк и сравнить наклоны. На бумаге разделяет прекрасно
  (0.00003…0.00116 px/строку у четырнадцати сканов против 0.0136…0.123 у
  схлопнувшихся), но пол у статистики — это несократившееся содержание, и на
  коротком листе он взлетает: 0.0035 на одиннадцати полосах шума, 0.0144 на
  собственном листе selftest'а с `--rows 2`. Против 0.0136 у самого слабого
  обрыва. Порога между ними нет.
- **Парный t по полосам** (у каждой полосы своя верхняя и нижняя половина,
  среднее разности против её же разброса). Хорош в теории — содержание у
  каждой полосы своё, геометрия общая, — и бесполезен на практике: честные
  бумажные сканы дают t от 1.6 до 123, потому что настоящая бумага
  действительно гнётся, и гнётся систематически. Разделения нет вовсе.

Обе меряли ту же величину, что и сама оценка, тем же способом — центроидом
чернил, — и потому наследовали её зависимость от содержания.

### 14.3 Что заработало: собственный край чернил

Накренённый лист кренит и свой чернильный блок. Крайние засвеченные колонки в
верхней восьмушке листа против тех же в нижней — это и есть сдвиг, в пикселях,
без единой ссылки на то, что напечатано. Где оценка — содержание, край —
геометрия.

Каждый край сверяется **сам по себе, и побеждает ближайший**: у `sheetB_scan` в
верхней полосе сидит пятно, которое уводит левый край на 1343 px, тогда как
правый ложится в 3 px от оценки. Расходиться с оценкой должны оба.

| | расхождение, px сдвига |
|---|---|
| восемь реальных сканов, 61…242 полосы | 0.1…3.8 |
| синтетика, где оценка верна | 0.0…2.3 |
| оценка занижает (36 px сдвига) | 2.1…20.5 |
| **оценка схлопнулась (48…80 px)** | **45.5…71.8** |

`SKEW_GAP = 15 px` — вчетверо над худшим честным листом и втрое под самым
слабым обрывом. Ловит каждый обрыв, что здесь получился, и промежуточную
стадию — только иногда; это правильная сторона: занижение стоит 0.01 от r,
обрыв — всего r.

### 14.4 Регрессия

- Пятнадцать листов прочитаны — ни одного предупреждения.
- Оценки не двинулись ни на знак: B 0.9683, C 0.9712, D3 0.9757, D4 0.9843,
  01 0.9436, 02 0.9124.
- Selftest: три честных листа обязаны молчать, а лист на 48 px сдвига —
  прочитаться ниже r 0.5 **и** сказать об этом. Оба selftest'а зелёные.
- Проверка живёт в `lane_drift()`, через которую ходят оба ридера, так что
  `cut_lanes` получает её даром.

Не сделано: сама починка. Подгонка по полосам строк вместо всего листа — то,
что стоит писать в день, когда предупреждение сработает на настоящем листе.

---

## 15. 29.08, ночь: резак падал на двух реальных сканах

`cut_lanes.py sheetD_scan1.png` и `_scan2.png` кончались сырым трейсбеком PIL
(`UnidentifiedImageError`) прежде, чем резалась хоть одна полоска. Это те самые
два файла, у которых сканер написал `pHYs` с неверным CRC (§13.3).

Причина — вторая дверь в тот же файл. Чернила резак берёт через `load_ink`,
которая чинит чанк в памяти и говорит об этом, а **dpi он спрашивал у PIL
напрямую** — в `strip_dpi()`, ради того чтобы полоска печаталась в натуральную
величину. Ровно тот чанк, из-за которого файл и не открывается. Починено там
же, через `mend_png`: `load_ink` про починку сообщает, `strip_dpi` молчит,
потому что добавить ей нечего.

Прогнано после починки, ни одной ошибки: двенадцать сканов (01, 02, 03, 04,
04_1, 05, `sheetB_scan`, `sheetC_scan`, `sheetD_scan1…4`), три цифровых листа
(B, C, D), `04.png --upside-down` и два листа одной командой с продолжающейся
нумерацией. `sheetD_scan1` при этом честно называет себя: «pHYs mended»,
«two rows to the sample», 120 полосок.

Selftest резака получил этот случай: у собственного листа ломается CRC на
`pHYs`, и полоски обязаны выйти **теми же пикселями**, что с целого листа, а
починка — сказать о себе. Проверено, что проверка ловит: со старым `strip_dpi`
selftest падает тем же `UnidentifiedImageError`.

Заодно переписан `ponytail:`-комментарий в selftest'е `paper_sound` — тот, что
называл запас перекоса двукратным, сравнивая 17.67 px с 24. По §14.1 предел
стоит в пикселях сдвига (24 точно, 36 занижает, 48 ноль), здешние листы лежат
на 3…36 px, и `sheetC_scan` уже стоит там, где оценка занижает.

---

## 16. 30.08: подгонка по полосам строк — и потолок, который оказался резкой

Апгрейд, который числился в §4 и в обеих README с самого начала: подогнать снос
не одной прямой на лист, а по полосам строк. Написан, померен, и половина
ожиданий не подтвердилась.

### 16.1 Сама по себе она не работает

Прогон на цифровых листах в 6570 строк, r после чтения:

| сдвиг | одна прямая | прямая ×3 | 8 полос ×1 | 8 полос ×3 | 24 полосы ×2 |
|---|---|---|---|---|---|
| 24 px | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.0000 |
| 36 px | 0.9997 | 1.0000 | 0.9954 | 1.0000 | 1.0000 |
| **48 px** | 0.0895 | 0.0896 | 0.2109 | 0.2148 | 0.2332 |
| 80 px | 0.0960 | 0.1292 | 0.2056 | 0.1975 | 0.1656 |

Обрыв не берёт никакая. И на бумаге дробление вредит: `sheetC_scan`
0.9704 → 0.9377 с итерациями, `sheetD_scan3` 0.9757 → 0.9089 на восьми полосах.
Полосы ловят остаток содержания, который среднее по ланам не сократило, и
принимают его за геометрию.

Причина, почему итерации не помогают: **первый проход идёт вслепую**. Окно
следует за штрихом, и на кривом листе оно теряет его в первом же проходе; всё,
что подгоняется дальше, подгоняется к потерянному штриху. Уточнять нечего.

### 16.2 Потолок — это шаг полосы, и он в резке

> Снято в §19: профиль колонок теперь суммируется вдоль наклона, и та же
> синтетика читается на 1.0000 вплоть до 16°. Таблица ниже — как было.

Прогон с мелким шагом по сдвигу, 11 ланов, 6570 строк, шаг 38.7 px:

| сдвиг | /шаг | полос нарезано | r, одна прямая | r, наведённая + полосы |
|---|---|---|---|---|
| 30 px | 0.78 | 11 | 1.0000 | 1.0000 |
| 36 px | 0.93 | 11 | 0.9997 | 1.0000 |
| 38 px | 0.98 | 11 | 0.9988 | 1.0000 |
| **40 px** | **1.03** | **12** | 0.2703 | 1.0000 |
| 42 px | 1.09 | 12 | 0.0023 | −0.0176 |
| 48 px | 1.24 | 12 | 0.0895 | −0.0569 |

Сетка ломается ровно на шаге: на 40 px лист режется на двенадцать полос там,
где напечатано одиннадцать. Полоса вверху листа стоит над колонками соседней
внизу, и **вертикальный рез не может их разделить** — это тот же потолок, что
§5.4 намерил для резака («с ~30 px лан гуляет шире собственного шага»), только
теперь он назван числом и относится ко всему ридеру.

Значит, прежняя формулировка «оценка схлопывается на 48 px» была про симптом.
Схлопывается она потому, что лист к тому моменту уже не режется.

### 16.3 Что заработало: навести первый проход по краю чернил

Край чернил меряет сдвиг геометрически, при любой кривизне (§14.3). Отдать это
число первому проходу как стартовый снос — и окно больше не ищет штрих вслепую.
В таблице §16.2 это столбец «наведённая»: **40 px читаются на 1.0000 вместо
0.27**, и вплоть до потолка оценка перестаёт занижать вовсе.

Поверх наведённого прохода полосы строк уже не вредят, а чуть помогают. На
бумаге, r против напечатанного:

| лист | одна прямая | наведённая + полосы |
|---|---|---|
| `sheetB_scan` | 0.9683 | 0.9683 |
| `sheetC_scan` | 0.9712 | 0.9704 |
| `sheetD_scan1` | 0.9784 | 0.9781 |
| `sheetD_scan2` | 0.9810 | 0.9814 |
| `sheetD_scan3` | 0.9757 | **0.9785** |
| `sheetD_scan4` | 0.9843 | 0.9844 |
| `01.png` | 0.9436 | 0.9445 |
| `02.png` | 0.9124 | 0.9141 |

Пять вверх, два вниз, один вровень; самое большое движение в любую сторону —
0.0028, то есть 0.13 dB на `sheetD_scan3`, самом кривом из здешних. Разложено
по половинам на трёх листах: наведение несёт весь +0.0028 у D3 и весь −0.0008 у
C, полосы несут +0.0019 у 02. Число полос — 1, 4, 8, 16 — держится в пределах
0.0002, то есть ручка это не настроечная; в коде стоит 8.

Цифровые потолки не сдвинулись: `--rows 1` 32.64 dB как было, `--rows 2`
25.10 → 25.11.

### 16.4 Предупреждение переехало на потолок

Проверка «оценка разошлась с краем» (§14.3) с наведением почти теряет смысл —
подгонка теперь стартует от того самого края. Вместо неё лист говорит про то,
что действительно катастрофично: **сдвиг дошёл до шага полосы**. Это одна
величина, снятая с самого листа, и она предупреждает раньше, чем сетка начнёт
считать лишние полосы. Проверка «фит ушёл от края больше чем на 15 px» осталась
как сторож самой подгонки: полосам разрешено следовать за листом, но не
уходить от него.

Selftest: три честных листа обязаны молчать, а лист на 48 px сдвига при шаге
39 — сказать про сдвиг и прочитаться хуже 0.9 (теперь он читается на 0.59, а не
на 0.07: наведение вытаскивает даже лист за потолком, но верить ему нельзя, и
проверяется именно то, что об этом **сказано**).

### 16.5 Что осталось

Косая резка полос. Всё, что здесь сделано, живёт под потолком в один шаг;
поднять потолок можно только тем, чтобы резать ланы по наклонным колонкам —
`find_tracks` и все кропы тогда перестают быть прямоугольниками. Это большая
работа, и ни один здешний лист её пока не требует: самый кривой стоит на 0.8
своего шага.

---

## 17. 30.08: три вопроса и одна ручка

### 17.1 Сколько градусов перекоса лист терпит

> Снято в §19 вместе с потолком §16.2. Градусы ниже теперь считаются от
> стекла, а не от шага полосы: A4 на A4 перестаёт влезать около 1.2°.

Потолок §16.2 — один шаг полосы сдвига — в градусах, при умолчаниях (A4,
600 dpi, шаг 38.7 px, 6780 строк чернил): **0.33°**, то есть **1.6 мм** между
верхом и низом листа. Пропорционален шагу: 77.4 px терпит 0.65°, 18.4 px —
0.16°.

| лист | сдвиг | градусы | мм |
|---|---|---|---|
| 03 | 2.8 px | 0.024° | 0.12 |
| 01 | 4.1 | 0.036 | 0.17 |
| 05 | 4.9 | 0.043 | 0.21 |
| 02 | 6.2 | 0.054 | 0.26 |
| 04_1 | 7.5 | 0.065 | 0.32 |
| 04 | 9.4 | 0.082 | 0.40 |
| D1 | 18.8 | 0.164 | 0.80 |
| D4 | 22.6 | 0.197 | 0.96 |
| D2 | 28.3 | 0.247 | 1.20 |
| D3 | 29.8 | 0.260 | 1.26 |
| B | 30.8 | 0.269 | 1.30 |
| C (шаг 77.4) | 35.8 | 0.312 | 1.52 |

Самые кривые здешние листы стоят на **80% бюджета**, а на глаз 0.3° не видно.
Отсюда строчка в обеих README: класть лист вплотную к направляющей.

### 17.2 Писк — свойство стекла, померено ещё раз

Заново, на сканах, которых в §3.8 не было:

| чтение | пик | период | превышение |
|---|---|---|---|
| 01 (600 dpi, `--rows 1`) | 823.4 Гц | 7.98 строк | +15.5 dB |
| D3 (600 dpi, `--rows 2`) | 1646.4 Гц | 3.99 отсчёта = 8.0 строк | +18.9 dB |
| D4 (600 dpi, `--rows 2`) | 1646.6 Гц | 4.00 отсчёта = 8.0 строк | +14.9 dB |
| D1 (602 dpi, `--rows 2`) | 1646.7 Гц | 4.01 отсчёта = 8.02 строки | +18.3 dB |

Период держится на 8.00 строках через две сессии, два режима печати и две
настройки dpi. В звуке тон садится на одну восьмую частоты строк бумаги
(`f = строк бумаги в секунду / 8`), поэтому на `--rows 2` он вдвое выше, и
поэтому же его двигают формат бумаги и dpi, а не принтер и не плотность
печати — чернила меняют только громкость (+15…+28 dB в чернилах против +10 на
голой бумаге, §3.8).

Мелочь на грани разрешения: 8.02 строки на 602 dpi против 7.98–8.00 на 600 —
ровно то, что дало бы фиксированное расстояние на стекле (≈0.34 мм), а не
фиксированное число шагов сенсора. Гипотеза, не факт: разница 0.3%.

### 17.3 Часы на узком лане: пункт спит

Промах 1–2 отсчёта на лан (§3.3) появляется только при шаге 18.4 px. Печатать
плотнее 38.7 нельзя по другой и более весомой причине — −4.0 dB (§3.2), — так
что узких часов на листе просто не бывает. Чистота листа тут ни при чём.

### 17.4 `bench.py --same`: медиана сканов вместо одного скана

Долг §4/§13.1 закрыт ручкой. Флаг говорит, что сканы — это один лист, снятый
несколько раз, а не страницы записи; под построчными оценками печатается
медиана и разброс:

```
python bench.py --same scanA.png scanB.png scanC.png song.wav
```

Протокол: три скана, **поднимая и заново укладывая лист между ними**. Укладка —
половина того, что усредняется, и лист, оставленный на стекле, даст три раза
одну и ту же укладку.

Первый прогон, четыре скана листа D:

```
sheetD_scan1.png     r=0.9781
sheetD_scan2.png     r=0.9814
sheetD_scan3.png     r=0.9785
sheetD_scan4.png     r=0.9844
median of 4 scans    r=0.9800  13.85 dB   spread 0.9781 to 0.9844, 1.52 dB
```

**13.85 dB** — это и есть число листа D, которым его надо сравнивать с
контролем B (11.78), то есть **+2.1 dB**: ровно то, что §13 насчитал медианой
вручную, теперь одной командой. Разброс при этом сжался с 1.95 dB (§13.1) до
1.52: наведение первого прохода (§16.3) отняло часть той самой лотереи, потому
что часть разброса была разной кривизной укладки, а не рябью каретки.

Порог «стенд не видит разницы меньше 2 dB» из §4 остаётся, но мерить его
теперь есть чем.

---

## 18. 30.08, вечер: перекос дальше — разбор, без кода

Не сделано, зафиксировано, чтобы продолжить. В папке `deskew_c/` (не в
индексе) лежат три программы на C: `deskew.c`, `deskew_bilinear.c`,
`deskew_bicubic.c`. Все читают 8-битный PGM, поворачивают на **заданный
аргументом** угол и пишут PGM; отличаются только интерполяцией — ближайший
сосед, билинейная, бикубическая (сплайн a = −0.5, clamp по краям). Угол ни
одна не измеряет, а мы его теперь меряем (`ink_lean`, §14.3).

### 18.1 Что именно ломается от перекоса — только профиль колонок

Горизонтальную часть сноса ридер уже отрабатывает окном, едущим по ramp'у, без
единого пересчитанного пикселя; вертикальную снимает `ink_rows`, кропящий
каждый лан по его собственным строкам чернил. Единственное место, которое
перекос убивает, — **профиль колонок в `find_tracks`: он суммируется строго
вниз.** Когда лан внизу листа заезжает на колонки соседа вверху, периодика
размазывается, и сетка считает лишние полосы (§16.2). Это и есть весь потолок
в 0.33°.

### 18.2 Поворот образа платит той монетой, из которой сделан носитель

- **Ближайший сосед дисквалифицирован.** Он квантует позицию к целому пикселю,
  а носитель — это субпиксельная позиция, живущая в краевых пикселях
  частичного покрытия. Тот же механизм, что у белой точки, и та стоит 8.6 dB.
- **Билинейная и бикубическая линейны по яркости**, а центроид — линейный
  функционал, поэтому саму позицию они сохраняют. Платят вертикальным
  размытием — статья, которая в README стоит 1.9 dB и обходится только
  `--rows 2` ценой половины листа. Бикубика размывает меньше и звенит на
  резких переходах, то есть ровно там, где ридер и так теряет (§6.2, крутизна).
- Мелочи: только PGM и только 8 бит (сканы надо конвертить); в `read_token`
  из `deskew.c` буфер на 256 без проверки границ.

### 18.3 Дешёвая альтернатива: суммировать профиль вдоль наклона

Сдвинуть каждую строку на −lean·y **перед суммированием профиля** — индексная
арифметика, ни одного пересчитанного пикселя, ни децибела потерь. Наклон уже
измерен геометрически, кроп лана уже расширяется на `reach`, окно уже едет по
ramp'у: недостающий кусок — только профиль, порядка двадцати строк в
`find_tracks`.

### 18.4 Насколько дальше можно уехать

| | предел | во сколько раз |
|---|---|---|
| сейчас | 0.33° — один шаг полосы, 1.6 мм | — |
| наклонный профиль | ~2–3°: при 3° чернила 199×287 мм дают 214 мм в ширину, на A4-стекле в 216 мм ещё влезают | ×7–9 |
| поворот образа | тот же геометрический предел, минус размытие интерполяции | ×7–9, с потерей |

Потолок в обоих случаях упирается в стекло, а не в формат: бумага A4 целиком
перестаёт влезать примерно на 1.2°, чернила — около 3°. Дальше нужен сканер
побольше, а не другой алгоритм.

### 18.5 Что делать дальше, в этом порядке

1. **Прототип наклонного профиля** на синтетике: листы на 40, 60, 120 px
   сдвига, которые сейчас читаются на 0.27 / 0 / 0 — вернутся ли к 1.0000.
   Если да, пункт 2 неинтересен.
2. **Сколько стоит поворот.** `sheetD_scan4` (0.20°, 0.9844) повернуть бикубикой
   на его же измеренный угол и перечитать. Потеря в пределах 0.001 — поворот
   годится как запасной путь для листа, который нельзя переснять (снимок
   телефоном, чужой архивный скан); 0.01 и больше — только для безнадёжных.

Оценки §18.1–18.4 сделаны по механизму, не измерены. Ни одна цифра отсюда не
годится для README, пока не пройдёт через бумагу или через синтетику.

## 19. 30.08, ночь: наклонный профиль сделан — потолка в ридере больше нет

Пункт 1 из §18.5, целиком, на синтетике и на бумаге. Пункт 2 (сколько стоит
поворот образа) остаётся неинтересным ровно по той причине, которую §18.5 и
называл: наклонный профиль вернул листы к 1.0000, поворачивать нечего.
`deskew_c/` так и лежит вне индекса.

### 19.1 Что сделано

`read_tracks.col_profile(ink, lean)` — профиль колонок, суммируемый со сдвигом
каждой строки на −lean·y, центрированный на середине листа (там же, где ноль
у рампы `lane_centroid`). `find_tracks` меряет наклон сам (`ink_lean`, который
переехал сюда из `paper_sound` — это растровая геометрия), `find_pitch`
спрашивает тот же профиль про гармонику; спектральная половина `find_pitch`
и так была нечувствительна к перекосу, она построчная.

Двадцать строк, как и оценивалось в §18.3. Ни одного пересчитанного пикселя.

### 19.2 Синтетика: A4 при 600 dpi, 6780 строк, шаг 38.7

| сдвиг | до | после |
|---|---|---|
| 38 px | 1.0000 | 1.0000 |
| 40 px | 1.0000 | 1.0000 |
| 42 px | −0.0071 | 1.0000 |
| 240 px | мусор | 1.0000 |
| 1920 px | мусор | 1.0000 |

1920 px на 6780 строках — это 16°. Лист столько не вынесет физически: A4,
повёрнутый на A4-стекле, перестаёт влезать около 1.2°, один чернильный блок —
около 3° (§18.4, геометрия, не измерение). То есть **потолок вышел из ридера
и упёрся в стекло**, как §18.4 и предсказывал, только дальше: там ждали ×7–9,
получилось столько, сколько ни один сканер не даст проверить.

Обрыв «до» здесь на 42 px, а в таблице `lane_drift` — на 40 px: там лист был
6570 строк, здесь 6780. Механизм тот же, число зависит от геометрии листа.

### 19.3 Бумага: ничего не сдвинулось

| скан | до | после |
|---|---|---|
| sheetB_scan | 0.9683 | 0.9683 |
| sheetC_scan | 0.9704 | **0.9676** |
| sheetD_scan1 | 0.9781 | 0.9781 |
| sheetD_scan2 | 0.9814 | 0.9814 |
| sheetD_scan3 | 0.9785 | 0.9785 |
| sheetD_scan4 | 0.9844 | 0.9844 |
| 01.png | 0.9582 | 0.9582 |
| 02.png | 0.9025 | 0.9025 |

Семь из восьми совпали до четвёртого знака, C потерял 0.0028 — 0.12 dB против
1.95 dB между двумя сканами одного и того же листа (§13.1). Это шум стенда, а
не эффект; но C — единственный лист здесь с шагом 77.4 px и наклоном в полшага,
и если он ещё раз просядет на чём-нибудь, смотреть надо туда.

### 19.4 Ловушка, которая стоила sheetB всего чтения

Профиль надо делить на **число строк, реально попавших в колонку**, а не на
высоту листа. Иначе крайние колонки, куда доехала только часть строк,
проваливаются в яму, а `prof.min()` — это тот самый пол, относительно которого
`find_tracks` считает загрузку полосы. Яма опускает пол на треть чернил, каждая
ячейка голой бумаги перебирает порог 0.25 медианы, и скан с мусором сбоку от
поля режется на 95 полос вместо напечатанных 59 (r = 0.0071).

Это ровно тот же sheetB, чей левый край врёт на 1343 px в `ink_lean` (§14.3):
лист с мусором на поле находит каждую дырку по очереди.

### 19.5 Предупреждение снято

`lane_drift` больше не печатает «this sheet is sheared … no vertical cut can
separate them»: потолка, который оно называло, нет. Проверка в селфтесте,
которая требовала, чтобы лист на 48 px сдвига развалился и сказал об этом,
перевёрнута — теперь она требует, чтобы 48 px и 300 px читались целиком и
молча. Второе предупреждение про перекос (`SKEW_GAP`, §16.4 — подгонка ушла с
листа) осталось: оно про сломанное измерение, а не про лист за пределами
формата.

### 19.6 Что осталось

- Проверить на бумаге, что потолок и правда стеклянный: напечатать лист,
  положить его на стекло под 2–3° и прочитать. Все цифры §19.2 — синтетика.
- C и его 0.0028. Одиночный скан, одиночный лист; `bench.py --same` (§17.4) на
  трёх пересъёмках C сказал бы, шум это или нет. Переснять C всё ещё некому.

## 20. 30.08, ночь: разбор со стороны — что взято, что померено и отклонено

Внешний обзор кода. Ниже — только то, что проверено на этом коде: пять правок
внесено, одна рекомендация померена и отклонена, две проверены и оказались уже
сделанными, остальное — упаковка, которой этот инструмент не хочет.

### 20.1 Взято

| | что было | что стало |
|---|---|---|
| стык листов | шов между листами не попадал в `joins` | `lane_joins()`, шов = стык как любой другой |
| нормировка печати | по пику | по 99.9-му перцентилю, тот же `HEADROOM` |
| `despeckle` на тишине | медиана шага 0 → порог 0 → чистит всё | выходит, не тронув |
| ориентация | правило написано дважды | `clock_order()`, оба ридера зовут её |
| 16-битный скан | `convert("L")` клипует → лист читается пустым | `ink_of()` масштабирует |

**Шов между листами.** `range(at + lane, at + len(piece), lane)` останавливается
на шаг раньше конца листа: лист из n ланов кончается на `n*lane`, а это то
место, где обход встаёт, а не шаг, который он делает. Все стыки внутри листа
сглаживались, стык между листами — нет. Это один щелчок примерно раз в две
минуты, ровно на тех чтениях, которые слушают целиком.

**Нормировка печати** — единственная правка, которая меняет то, что выходит на
бумагу. Экскурсия и есть носитель, а нормировка по пику отдаёт весь бюджет
самому громкому одиночному сэмплу. Через модель скана (blur 5, шум 10/255),
r против напечатанного:

| источник | по пику | по перцентилю |
|---|---|---|
| чистый | 0.9322 | 0.9318 |
| один щелчок, музыка на 0.15 | 0.8953 | 0.9317 |
| полный размах | 0.9322 | 0.9318 |

+2.1 dB там, где это важно, −0.02 dB там, где нет. **На безшумном рендере
эффекта не видно вообще** (0.9993 против 0.9998): носитель там может быть
сколь угодно мелким и всё равно читается идеально. Это ровно та ловушка, о
которой журнал говорит с §2.

**16 бит.** Проверено на градиенте 0…65535: `convert("L")` не масштабирует, а
клипует на 255, и лист приходит как [255, 0, 0, 0] — первый пиксель и дальше
пустота. Сканер, поставленный на 16 бит, — обычное дело, а README просит серый
и не говорит «8 бит». Так лист терялся молча.

### 20.2 Померено и отклонено: порядок `declick` / `highpass`

Рекомендация была `despeckle → declick → highpass` вместо нынешнего
`declick(highpass(despeckle))`. Довод: MA-фильтр на 331 отсчёт размазывает
ступеньку стыка, и declick потом чинит уже испорченное.

Померено на 12 ланах с собственным смещением у каждого (это и делает стык
ступенькой):

| порядок | худшая ступенька на стыке | r против источника |
|---|---|---|
| нетронуто | 0.48116 | — |
| declick после highpass (как есть) | **0.00000** | 0.964298 |
| declick до highpass | 0.00285 | 0.964303 |

Нынешний порядок точнее, и по механизму: declick меряет и убирает **ту**
ступеньку, которая останется в файле. Сделай наоборот — highpass после него
вернёт маленькую ступеньку обратно, потому что скользящее среднее по обе
стороны стыка разное. Оставлено как есть.

### 20.3 Проверено — уже сделано

- **`lay_out` на коротком хвосте.** `do_print` паддит чанк до целых секунд
  (`np.pad`), лист всегда целое число ланов. Через CLI короткий хвост не
  доходит.
- **`--dpi` не масштабирует штрих.** Знает и говорит: при штрихе тоньше
  0.15 мм печать печатает NOTE и называет, какие `STROKE` и `--pitch` вернут
  лист к геометрии 600 dpi. Физические единицы вместо пиксельных — это другой
  формат, а не правка.

### 20.4 Не взято

- **ECC, заголовок, контрольная сумма.** Формат — картинка собственного
  содержимого, это его свойство, а не недоделка.
- **Пакет, типы, `logging`, прогресс-бар, тесты отдельным файлом.** Приборный
  инструмент; `selftest` рядом с форматом — это то, что делает его правдой.
- **Вынести `ponytail:` пометки и 65-строчный docstring модуля.** Пометки
  ведут журнал долга, а docstring — то, что видно в `--help`, и это выбрано.
- **`open(path, "rb").read()` в `mend_png` без `with`.** Верно формально;
  строка на файл в программе, открывающей файл на лист.

### 20.5 Осталось открытым

- `cut_lanes` считает `lane_drift` дважды — в `clocks()` и в `traces()`, с
  разной апертурой, поэтому это не просто дубль. Проход по листу стоит
  секунд; на пачке листов это заметно.
- Внешний цикл `despeckle` заходит в burst, который уже переехал по `reach`.
  Не тронуто нарочно: таблица в этом docstring померена с таким поведением, и
  смена смысла `reach` обесценит её.
- 16 бит сведены в 8. Стоят ли лишние биты чего-нибудь — не спрошено; строка,
  которую менять, помечена.

## 21. 30.08, ночь: полосы `sheetD_scan2` через `picky.py` — где терялся звук

Вопрос со стороны: нарезали `sheetD_scan2.png` в полосы, воспроизвели их
`picky.py`, вышло шумно. Резак ли виноват, `--rows 2` ли.

**Резак ни при чём.** Те же пиксели: `paper_sound` читает этот скан на
медиане 0.9818 по секундам, а простой центроид полос — на 0.930 по
полусекундам. Терялось всё в воспроизведении, и потери оказались тремя
разными, независимыми.

### 21.1 Лестница потерь

Всё против `src59.wav`, медиана r:

| | r | dB |
|---|---|---|
| `paper_sound read` | **0.9818** | 14.3 |
| простой центроид полос, по одной | 0.930 | 8.1 |
| `picky` с исправленными аргументами и снятым дрейфом | 0.8774 | 5.2 |
| `picky` как было запущено | **0.0362** | −29 |

### 21.2 Три причины, по убыванию цены

1. **Полшага сэмпла на полосу.** Полосы вышли 6609 строк — при двух строках на
   сэмпл это 3304.5 сэмпла, то есть полоса не содержит целого числа сэмплов.
   Тот, кто склеивает полосы подряд, уносит эти полсэмпла в следующий лан: один
   сэмпл в секунду, 59 сэмплов к дальнему краю листа. Корреляция против
   источника решается единичными сэмплами сдвига, поэтому **та же самая запись
   даёт 0.0362 с этим дрейфом и 0.8774 без него**. Починено в резаке: высота
   полосы округляется вниз до целого числа сэмплов (§21.3).
2. **`--rows 2` не поделены.** Полоса — картинка СТРОК, а на этом листе две
   строки = один сэмпл. Файл вышел 122.9 с там, где на листе 59 с. Ридер,
   берущий строку за сэмпл, надо запускать на **13218 Hz** (2 × 6609), а не на
   6500. `picky` этого знать не может, и сам лист об этом молчит — говорят
   только часы, которые резак и читает.
3. **20-герцовый ФВЧ `picky` на каждую полосу** (двойка в множителе `330`).
   Померено на здешних полосах: 0.9333 без него, 0.7334 с ним. Он причинный,
   второго порядка, и на каждой полосе начинается заново — переходный процесс
   в начале каждой полусекунды. Степень интенсивности (`3`) не значит ничего
   (0.9333 / 0.9326 / 0.9322 для 1, 2, 3), выравнивание концов (`3` в
   множителе) — тоже ничего (+0.0008).

То есть рабочая команда — `picky.py x 50 3 d 165 1 0.95 pdp 13218 24 "…/*.tif"`
(множитель без двойки, частота строк вместо частоты звука), и полосы, нарезанные
после §21.3.

### 21.3 Что исправлено в резаке

- **Высота полосы округляется до целого числа сэмплов**, когда часы говорят
  больше одной строки на сэмпл. Полосы и так растягиваются к общей высоте, так
  что это стоит одной строки картинки. Селфтест режет лист, у которого ланы
  нарисованы НЕЧЁТНЫМИ по высоте, и требует чётных полос.
- **Строка под нарезкой называла не ту частоту.** Было: «одна полоса 6609
  строк и держит 1/2 секунды, значит звук на ней 13218 Hz». Звук на ней 6609 Hz
  — вдвое меньше сэмплов в половине секунды; 13218 — это частота СТРОК, то есть
  как раз то, что нужно `picky`, но названо оно было звуком. Теперь называются
  оба числа, каждое своим именем.

### 21.4 Потолок этого пути

Даже сделанное правильно, чтение полос упирается в 0.88–0.93 против 0.98 у
`paper_sound`. Разница — то, чего в полосах нет:

- **часы**. На листах D ретайминг по часам стоит +1.4…+4.9 dB (§README), и в
  картинке полосы его нет вообще;
- **пересэмплирование лана к общей длине** — резак делает это с КАРТИНКОЙ, но
  дальше каждый лан живёт своей длиной в сэмплах;
- **узкое липкое окно** — центроид полосы берётся по всему кадру, маска
  апертуры (`APERTURE_MASK`) лишь ограничивает его геометрически.

Полосы — это диагностика и материал для чужого ридера, а не второй ридер. Для
звука с листа читать надо `paper_sound`.
